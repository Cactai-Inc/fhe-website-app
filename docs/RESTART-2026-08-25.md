# RESTART — read this first

**Written 2026-08-25, immediately before a context compaction.** Everything needed to resume without
the conversation. **Nothing here requires the thread it came from.**

---

## 0. WHAT WE ARE DOING

The owner is running a **full-repo remediation pass on FHE before a globalization refactor**, in his
words *"to get as much of the ui and ux in the right standing ahead of the final evaluation pass of
the repo so there is less guess work and less ambiguous questions for me to answer."*

**He finds issues. I hold the pen.** The output is not code in a thread — it is a set of documents
that survive the thread.

## 1. HOW WE ARE DOING IT — READ THE SOP BEFORE ANYTHING ELSE

**`docs/METHOD-change-orders.md`** — the six-step process, named by him:

| # | Step |
|---|---|
| 1 | Identify issues & author change requests |
| 2 | Research |
| 3 | Discussion & Lock |
| 4 | Author the handoff set for Architecture & Design |
| 5 | Review design deliverables + author the Build handoff |
| 6 | Review build deliverables + user validation testing |

⚠️ **THE THREE RULES I KEEP BREAKING, AND HE HAS CORRECTED ME TWICE ON:**
1. **During capture: nothing but the provocation.** No findings, no recommendation, no status.
2. **One at a time**, even when he sends ten things.
3. ⚠️ **Even when he asks a direct question**, the answer goes in the **ledger** and surfaces in
   step 3 — **not in the reply.** *"This thread is disposable"* — **anything that exists only in a
   reply is lost.**

⚠️ **WIDENING vs CLARIFYING.** A widening question asks him to look **ACROSS** — sibling controls,
other surfaces, whether the pattern repeats. A clarifying question asks him to look **DOWN** —
specify a detail, define a state. **Clarifying questions cost him the flow state he finds problems
in, and an important clarifying question is still the wrong question mid-capture.** Find the detail
in step 2 or file it as `ASK-OWNER`.

**The only two things worth saying during capture:** *"What else?"* or a **specific provocation
pointing him at something to LOOK at.**

## 2. THE DOCUMENTS — these are the work

| File | Holds |
|---|---|
| `docs/METHOD-change-orders.md` | the six-step SOP and the working rules |
| `docs/CHANGE-ORDER-LEDGER.md` | **80 change requests**, CR-01…CR-78 (incl. CR-76b), grouped G1–G9, each with `SAID` verbatim, `A/B`, `ASK-REPO`, `ASK-OWNER`, `FOUND`, `LOCKED` |
| `docs/STEP2-FINDINGS.md` | the research, in plain language, and the recommended step 3 order |
| ⚠️ **`docs/SPEC-first-contact-flow.md`** | **THE END-TO-END FLOW** — one check at send, a **computed checklist that resumes**, the disposition as the skip rule, and where she lands. ⚠️ **Supersedes the P1 handoff's item 1** |
| `docs/HANDOFF-P1-CONTRACT-SHIP.md` | the P1 build assignment — ⚠️ **read the SPEC above it** |
| `docs/reports/P1-REVIEW-2026-08-25.md` | the step 6 review of the P1 build, incl. the drift-guard finding the build thread disputes |
| `docs/tasks/TASK-DAYSHEET-…md` | the narrative working notes behind the ledger |

⚠️ **An OVERRIDDEN statement is DELETED, not archived** — *"proceed as if it was never mentioned."*
An **A/B he offered and has not chosen** is not an override and stays live.

## 3. WHERE WE ARE

- **Step 1 — reopened repeatedly; he adds as he finds.** 78 requests captured, audited complete against every message.
- **Step 2 — COMPLETE.** Findings written; the recommended step 3 order is at the end of
  `STEP2-FINDINGS.md`.
- **Step 3 — IN PROGRESS, 2 of 73 touched.**
  - ✅ **CR-27 LOCKED** with ten validation criteria — the order lifecycle.
  - 🔓 **CR-30 mid-lock** — the people surfaces. **Three questions still unanswered:**
    1. Does a **marketing-zone or dungeon entry carry the submission**, like a promoted client's
       record does?
    2. Is **Account History a tab** on the client record page, **or a page of its own**?
    3. Is **promote silent** until *send the activation link* — no email on promotion?
  - **Everything else is `captured` or `researched`.**
- ⚠️ **Step 3 was INTERRUPTED by a P1 escalation** *(§5)*. Resume at CR-30's three questions.

## 4. THE RULINGS THAT GOVERN EVERYTHING ELSE

Locked in step 3 and not to be re-litigated:

1. ⚠️ **APPROVING A REQUEST *IS* CREATING THE ORDER.** A submission is a request to purchase.
   **No order exists before approval.** Booking is a separate step after it. **Payment gates
   FULFILMENT, not booking.** The client never sees the word *approved* — they see *"your order has
   been processed"* and a payment request.
2. **There is no "decline".** A request is **changed** or **cancelled**.
3. ⚠️ **THE SURFACE RULE — modal, card and page each have a job** *(CR-74, settled 2026-08-25)*.
   **Do not move someone to see or edit something they are already looking at.**
   - **MODAL** — something quick, without leaving the page. ⚠️ **Never for content that would fill a
     page.**
   - **EXPANDED CARD, full width, in place** — **the record's own fields, viewed AND EDITED**, in the
     list you found it in. ⚠️ **Preferred over a deeper page showing the same information.**
   - **PAGE** — a record with **more than its fields**: the things attached to it.

   **Each record type gets its own view; the layout structure is shared.** Uniformity is the frame,
   personalisation is the content.
4. ⚠️ **A LEAD IS NOT A RECORD — IT IS AN INBOX ITEM.** No Leads tab. A dashboard notification opens
   the modal with the submission and its handling buttons. **Four exits:** make them a client ·
   marketable lead → marketing zone · cancel → the dungeon · **hard delete with an optional block.**
5. **The submission survives promotion** onto an **Account History** surface, which **replaces the
   Activity Log as a page**; the activity log becomes a link in its top-right.
6. ⚠️ **ONE RESOURCE, TWO VIEWS.** The client edits their fields on their own surfaces; staff see
   **the same record**. Not a copy, not a sync. **A client record page is never shared.**
7. **Timezone is `America/Los_Angeles`** — stored as tenant config, IANA name, never a fixed offset.
8. **Reminders: hourly cron, round the send DOWN.** At hour H, send for bookings starting in
   `[H+1, H+2)`. **Clients get one email** per send.
9. **`med_schedule` generates NOTHING** — the barn does not sell medication as a service. **Do not
   build a task generator from it.**
10. **"Visitor" replaces "guest"** everywhere.

## 5. THE P1 ESCALATION — what happened and what is still running

He was **blocked shipping a real client's lease** (Pamela Godde) and pulled us out of step 3.

**Already built, merged and DEPLOYED — `main` is `0b1b2bbe`:**
- a saved-but-not-sent client shows the horse card, so a horse can be added before the invitation
  exists
- the **emergency euthanasia block** is gone from the intake form, and the vet-auth template now
  **states** the position rather than asking the client to choose *(migration `20260825T1300`)*
- the **horse-information confirmation** is gone from **both** renderers, and its **lock blocker** is
  removed *(migration `20260825T1200`)* — ⚠️ **it had never once been satisfied and was refusing to
  lock every contract carrying horse fields**
- carried along: the PDF closing-section fix, the weekly-plan copy, the Orders tab rebuild
  *(+ migration `20260825T0900`)*, the provisioning offerings picker, paperwork rows

**Still open — handed to a separate build thread, RUNNING NOW:**
`docs/HANDOFF-P1-CONTRACT-SHIP.md`, worktree `../wt-p1ship`, branch `task/p1ship`, spawned with
Opus 5 / thinking ON / effort HIGH, instructed to **commit and NOT push**, reporting to
`docs/reports/P1-CONTRACT-SHIP-REPORT.md`.
1. **One email, not two** — account claim and contract in a single send
2. **Claim → fill only what is missing → straight into the contract**
3. **A party sees the rendered document**, not the authoring surface, updating immediately on a
   selection change

⚠️ **When that thread reports, step 6 applies: review its deliverables against the handoff's
validation criteria, then he validates.** **Do not push its work without him.**

## 6. THE PATTERN THAT EXPLAINS THIS CODEBASE

⚠️ **Nearly everything he asks for is BUILT AND UNDRIVEN.** Established in step 2:

| | |
|---|---|
| booking states | **8 of 12 never written** |
| request stages | **10 defined, 1 used** |
| line quantity | **wired through 14 functions, every row is 1** |
| member statuses | **3 allowed, 1 used** |
| **comping** | **complete — modes, reason, loss reporting** — reachable only for lesson credits |
| "My Orders" in the nav | **already there**, presence-gated |
| horse-owner controls | **2 of 3 already built** |
| the horse-confirmation gate | **built, wired, satisfied ZERO times, and blocking** |

**The work is mostly CONNECTION, not construction.** ⚠️ **So the first question about any request is
"does this already exist?"** — three of the P1 fixes were removals.

## 7. WHAT SUCCESS LOOKS LIKE

- **Immediate:** ⚠️ **Pamela Godde receives ONE email, claims her account, is asked only for what is
  missing, and lands on her lease reading the finished text.** That is the live test.
- **This pass:** all 73 change requests carried through steps 3–6 — each **locked with agreed
  validation criteria**, designed, built, and validated by him.
- **Then:** the **globalization refactor**, fed by G9, which step 2 already measured:
  **33 hand-rolled modal overlays vs 7 using the shared one** · **48 files hand-rolling the green
  button, 29 doing both** · **32 inline empty states** · **six corner radii while the design system
  has none** · **the same row list built six times** · **`initials()` written four times.**
- ⚠️ **Each group's fix should CARRY its globalization** — *"or the refactor inherits 34 pop-ups
  instead of 33."*

## 8b. ⚠️ STEP 3 PROGRESS SINCE THIS DOCUMENT WAS WRITTEN — 5 items touched, 1 locked

| CR | | State |
|---|---|---|
| **27** | the order lifecycle | 🔒 **LOCKED**, ten validation criteria |
| **30** | the people surfaces | 🔓 **three questions open** *(§3)* — ⚠️ **and partly overtaken by CR-75** |
| **74** | modal vs card vs page | 🔒 **RULED** |
| **75** | the client record becomes an expanding row, and the pattern nests | 🔒 **RULED**, testing is the next act |
| **76** | payments | 🔓 **ONE question open** — *does the payments page carry paid history, or only what is outstanding?* |

### 🔒 CR-74 — THE SURFACE RULE *(replaces "card = summary, page = the work")*
⚠️ **Do not move someone to see or edit something they are already looking at.**
- **MODAL** — quick, without leaving the page. ⚠️ **A modal CAN be the work** *(notifications open as
  modals; payment is exactly that shape — act, done, notification clears)*. **Never for content that
  would fill a page.**
- **EXPANDED CARD, full width, in place** — the record's own **fields, viewed AND edited**, in the
  list you found it in. **Preferred over a deeper page showing the same thing.**
- **PAGE** — a record with **more than its fields**: the things attached to it.

### 🔒 CR-75 — THE PATTERN NESTS, AND THE LIST BECOMES ROWS
- **Client records: condensed rows, client names.** ⚠️ He rates the desktop **grid** higher for
  density — **it loses anyway because it is not alphabetised and a grid has no reliable reading
  order.** Findability beat density.
- ⚠️ **This revises the model settled the same morning:** a client was to get a **page**; it is now an
  **expanding row**. **The lead half is untouched** — still a dashboard notification opening a modal.
- **Nesting:** client row → the horse inside it *(**yes**)* → a document inside that
  *(**"needs to be tested, i vote yes"**)*. **"Use this principle everywhere it works and see how it
  plays out."**
- **Closing:** ⚠️ **the CARD HEADER is the close** — it already opens and closes and *"works well"*;
  the top-level header closes everything. **Do NOT build a separate close button** *(he proposed one,
  then withdrew it)*. *"Much better UX than a back button 3 times."*
- ⚠️ **Closing SAVES.** Collapsing a parent takes its children for free; **committing edits on
  collapse does not — that is the build.**
- **Find the breaking point by building.** A **document** is the likeliest break *(pages of prose,
  its own scroll, three levels deep)*. **Mobile is one column below 1024px, so every level is the
  whole screen** — the line may fall elsewhere there.

### 🔒 CR-76 — PAYMENTS ⚠️ MY RECOMMENDATION WAS OVERRULED, AND HE WAS RIGHT
**Build the "My Payments" card AND page.** I argued against it as a third implementation and missed
the point:
> *"we wont mark it as paid until we see the zelle anyway, so might as well give them the power to
> change it so we dont have to. it saves time and discussions and confusion."*

- ⚠️ **THREE STATES, and CR-60's ladder is confirmed exactly:**
  **awaiting payment** *(the order exists, they have not chosen)* → **pending** *(⚠️ **they select
  their choice** — declaring they have paid **or will**)* → **paid** *(⚠️ **we verify the money
  arrived**)*.
  **A PENDING payment is CLIENT-EDITABLE** — they may switch Zelle to cash and back. **Awaiting
  payment is not "editable", it is unchosen.** ⚠️ **The trigger for pending is THEIR SELECTION, not
  our uncertainty.**
- ⚠️ **They may change the METHOD and nothing else** — *"there are only two choices for payment"*.
  Not the amount, **not who pays.**
- **Consolidate the two existing surfaces.** ✅ **Checked — neither is redundant:** *Manage payment*
  carries the **method + transferring who pays**; the order's own panel carries the **declaration
  with a memo/reference**, which is what matches it to money received. **One control on the payments
  surface, not a winner chosen.**

**⚠️ TWO LATENT BUGS FOUND, BOTH ON THE PATH HE JUST ASKED FOR:**
1. **`PAYMENT_METHODS = ['Zelle', 'Check', 'Cash', 'Card']`** — ⚠️ **`Check` and `Card` are not real
   options.** Production has only ever held `zelle` (5), `cash` (1), blank (2).
2. ⚠️ **CASING MISMATCH.** That dropdown writes **`'Zelle'`/`'Cash'`**; the declaration path and
   **every production row** are **lowercase**. **Using that dropdown writes a value nothing matching
   the lowercase form will find.**

---

## 8c. THE OTHER CHANGE REQUESTS CAPTURED SINCE — CR-66…CR-73
Nav toggles never landed *(66)* · the admin dashboard is unusable at every size *(67)* · the
client-facing Add-a-Horse modal, **including outside-click data loss** *(68)* · is the horse intake
form one component or three *(69)* · the horse record page: edit/remove gating, documents vs files,
a photos tab, feed tagging *(70)* · **horse activity limits driving availability** *(71)* · what the
four contract party controls do, and **"suggest" has never once been enabled** *(72)* · the
horse-accuracy block *(73, ✅ SHIPPED)*.

## 8. HOW TO RESUME

1. Read `docs/METHOD-change-orders.md` — the rules for how to behave.
2. Read `docs/CHANGE-ORDER-LEDGER.md` — what he has asked for.
3. Read `docs/STEP2-FINDINGS.md` — what is actually true, and the order to work in.
3b. ⚠️ **Read `docs/SPEC-first-contact-flow.md` before touching anything in P1.** It is the only
   place the full flow logic lives.
4. ⚠️ **Resume step 3 by CLOSING CR-76's last question** *(does the payments page carry paid
   history?)*, then **CR-30's three** *(§3)* — ⚠️ **re-read them against CR-75 first; the client
   surface changed after they were asked.** Then follow the recommended order:
   **CR-30 → pricing/cadences/comps → open slots + the toggle → the lead's world → the lying pages
   and flow exits → G9.**
5. ⚠️ **If the P1 build thread has reported, review it first** — it is unfinished business on a real
   client's contract.

⚠️ **`wt-dealparty` / `task/pagefit` is fully merged and can be removed.**

---

## 9. ⚠️ WHAT CHANGED AFTER THIS DOCUMENT WAS FIRST WRITTEN

**`main` has moved on** — several fixes shipped while step 3 was paused. Deployed and live:
- **the horse-record completeness** no longer counts euthanasia, a field the form no longer has *(it
  was leaving every horse permanently one short, with staff invited to chase it)*
- **the contract's horse picker** now follows the Lessor, reloads when the party changes, leads with
  the barn name and never shows the `N/A` sentinel — it had been offering **every horse in the org**
- **breed, colour and sex** render their menus on the horse-records card; they were bare text boxes
  over lookup **codes**
- **"Full Name (registered name, if registered)"** replaces "Registered name" on the three surfaces
  where the field is **entered** *(display labels and the contract token label deliberately unchanged)*

⚠️ **A PATTERN WORTH CARRYING:** three separate misses in one day were all the same shape — **a thing
was removed or changed in one place and a SECOND place that read it was missed.** The euthanasia
block *(form, template, completeness maths)*. The horse-confirmation control *(two renderers)*.
`remerge_contract_from_fields` *(two callers papering over it)*. **In this codebase, removing
something is a grep, not an edit.**

## 10. ⚠️ THE P1 BUILD THREAD HAS REPORTED — 9 commits on `task/p1ship`, STILL UNPUSHED

**The three assigned items are built** *(`d6372cc0`, `0c148427`, `22f9fc4c`)* and reported in
`docs/reports/P1-CONTRACT-SHIP-REPORT.md`.

### ✅ THE DISPUTE IS SETTLED — THE BUILD THREAD WAS RIGHT, MY REVIEW WAS WRONG
Commit `ddc144ba`. Verified independently on production, not taken on its word:
`remerge_contract_from_fields` ends its write with `AND workflow_state <> 'executed'`, and the drift
guard evaluates **only** when the document IS executed. **The destructive path I described needs
both at once.** My review read the RETURN value (`23`) and took it for the STORED value (`25739`).
Withdrawn, with the reasoning, at the foot of `docs/reports/P1-REVIEW-2026-08-25.md`.
⚠️ **The hazard it fixed instead is real**: two functions ENDED on a composer that reads
`contract_templates.body` — 23 characters for every clause-composed template. Safe today only
because all nine callers happen to recompose straight afterwards. **A property of nine call sites,
not of the function.**

### ✅ THE FAIL-OPEN RISK IS DISPROVEN
`caller_is_document_party` and `caller_party_roles` read the **same predicate**
(`document_parties.contact_id = current_contact_id()`), so a party who passes authorisation always
has roles and is always asked. **`my_roles: []` is reachable only by STAFF**, who correctly have
nothing of their own to give. Not a hole.

### ⚠️ FOUR COMMITS ARE BEYOND THE BRIEF AND ARE HIS CALL, NOT MINE
| Commit | | Size |
|---|---|---|
| `1a01b783` | the one-character-per-click horse editor bug | 2 files, ~170 lines |
| `7ef51ce6` | *"Other — enter manually"* adds to the menu; Haflinger spelling | 3 files + 2 migrations |
| `28e2ecb3` | ⚠️ **a global menus editor** — every dropdown in one editable place | **new page + 182-line migration** |
| `8f4a3bb3` | form fields editable/addable/removable, versioned on change | **2 migrations, ~520 lines** |

⚠️ **The last two are a standing D13 gap being closed off the back of a P1 hotfix.** Good work in the
wrong container — **they should not ride along on a contract-ship push.**

### ⚠️ A NEW FINDING — THE COMPANY HAS NO SIGNER NAMED
`LESSEE.ENTITY_SIGNER_NAME` and `LESSEE.ENTITY_SIGNER_TITLE` are **empty and both printed**, and
`LESSEE.PARTY_TYPE` is `ENTITY` — so the lease prints *By: ___ / Title: ___* for French Heritage
Equestrian **with nothing in it**. It is on OUR side, so **Pamela's intake will never ask for it.**
**Staff must fill it before this ships.**

### ✅ PAMELA'S ADDRESS GATE IS PROVEN AT THE DATA LEVEL
`document_parties` holds both parties · **Pamela is the LESSOR** *(so the horse questions are hers,
not FHE's)* · `LESSOR.ADDRESS` is **empty AND a printed token** → **the intake will ask her.**
⚠️ **Not proven at the UI level and cannot be** until she has an auth identity — the function is
caller-scoped, so rehearsing as admin exercises the staff branch.

**And the browser walk still has not been done.** Item 1 sends a real email to a real client.

### ⚠️ THE WHOLE FLOW IS NOW SPECIFIED — `docs/SPEC-first-contact-flow.md`
**Read it before touching P1. It supersedes the branching in the P1 handoff.** In short:

- **ONE CHECK AT SEND** — ⚠️ **does this person have an AUTH METHOD** *(has she ever logged in)*.
  **Not "does she have an account"** — a contract party always has one.
- ⚠️ **THE FLOW IS A COMPUTED CHECKLIST, NOT A FIXED WIZARD.** Built from what is actually
  outstanding, **rebuilt on login and on refresh**, and it **RESUMES** where she left off.
- **ORDER:** intake first when anything it collects is missing · the horse before a contract that
  needs one · **documents wherever their DISPOSITION puts them** · the contract in its place.
- ⚠️ **THE DISPOSITION IS ALSO THE SKIP RULE — it already exists.** `AT_LOGIN` · `WITH_CONTRACT`
  *(what puts documents before or after the contract)* · `WHEN_READY` *(dismissable, never
  blocking)*. **A dismissed item is NOT a completed one.**
- **THE END:** the last button sends **one email carrying the contract AND the documents** → the
  overview modal → then the **landing rule**: **dashboard when she has notifications, community feed
  when she does not.**
- **A horse created mid-flow ATTACHES ITSELF** to the contract and says so.

**The worked instance (Pamela) is §3 of the spec. The RULES that generate it are §3b and §3c — those
are the specification; the ten steps are one output of it.**

### ⚠️ PAMELA HAS AN ACCOUNT. WHAT SHE HAS NO RECORD OF IS AN AUTH IDENTITY.
> *"for the record pamela has an account. the question is does pamelas account have an auth method
> setup yet, if not then start with that flow. if it does then take her to the login screen and when
> she logs in she will be taken through the flow for the contract."*

⚠️ **THE REPO'S OWN RULE SAYS THE SAME:** *"Saving creates the account; sending the invitation is a
separate step."* **A draft invitation IS an account.** I had been calling the `profiles` check "does
this person have an account" — **it answers "can she log in", which is a different question**, and
the wrong name hid the branch that matters.

**Her exact state, verified:** account ✅ · invitation `draft` ✅ · **auth identity ❌** *(no
`profiles` row, no `auth.users` row)* · **no address** ❌

### 🔒 RULING — A CONTRACT PARTY ALWAYS HAS AN ACCOUNT (owner, 2026-08-25)
> *"everyone will have an account if im sending a contract to them unless they are email only and
> that method of contract creation doesnt work because the ui wants the email and full name to be
> able to create a contract, with that information i can create an account for someone."*

⚠️ **THIS COLLAPSES THE BRANCH THE P1 BUILD WAS WRITTEN AROUND.** Item 1 branches on *"does this
person have an account?"* — **and by this ruling the answer is always yes**, because creating the
contract party already required everything an account needs.

**Verified:** the underlying RPC **does** accept an email alone — it inserts a contact flagged
`name_needs_confirmation`. ⚠️ **But the UI demands email AND full name**, so the email-only path is
unreachable in practice, which is exactly his point. **With email and full name, the account can and
should be created there and then.**

**So the correct shape is:**
1. **Creating a contract party creates the account** — not a later branch discovering it is absent.
2. ⚠️ **The ONLY question left when sending is: does that account have an AUTH METHOD?**

**The branch, stated correctly:**
| | |
|---|---|
| **no auth method** *(Pamela today)* | **start with the set-up-your-login flow** — ⚠️ **this is the branch, NOT "no account"** |
| **has an auth method** | **the login screen** — and after signing in, into the contract flow |
| **either way** | ⚠️ **the intake for missing information comes BEFORE the contract is shown** |

⚠️ **THE ONE PIECE HE WANTS PROVEN, AND WHY IT IS NOT YET PROVEN:**
> *"the one piece we need to make sure is working properly is the intake form capturing the missing
> information from her before the contract is shown. right now she doesnt have an address so the
> contract has no address and that is needed before she reviews it."*

**`contract_intake_requirements` is live on production** and is **caller-scoped** — it reports what
**the signed-in person** is missing. ⚠️ **So it cannot be exercised as Pamela until she has an auth
identity**, which is exactly the thing she does not have. **Rehearsing it as an admin proves
nothing.**
⚠️ **AND A RESULT WORTH CHECKING BEFORE TRUSTING IT:** run as an admin who is not a party, it
returned **`"complete": true`** with **`"my_roles": []`**. **If "no roles found" produces "complete",
the gate FAILS OPEN** — someone with no matching party would pass straight through to the contract.
**Establish whether `complete` means "nothing is missing" or "nothing was checked" before this
ships.**

⚠️ **How the unified send works, confirmed in the code:** **send the CONTRACT, never the invitation.**
It checks whether the person has an account; if not it **reuses their existing saved invitation**,
stamps the document on it and sends **one** email carrying the account-claim link. The contract is
read **off the invitation row, not the URL**. Sending the invitation on its own gives them an account
with no contract attached.
