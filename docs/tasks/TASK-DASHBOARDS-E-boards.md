# TASK-DASHBOARDS-E — BOARDS: money registered once, the TODAY element advances, Sales + Marketing, then STOP; Admin desk independent; Ops composed LAST (CODR profile, three sub-chunks)

**Threads:** `FHE-TASK-DASHBOARDS-E1` (Sales + Marketing + the shared fixes), `FHE-TASK-DASHBOARDS-E2` (Admin desk), `FHE-TASK-DASHBOARDS-E3` (Ops — STUB, dispatched only after the owner's list) · **Profile:** CODR · **Bundle:** B7 · **Sender / hand back to:** `FHE-MGMT-DASHBOARDS` · worktrees assigned by MGMT; branches `task/dashboards-e1|e2|e3` from `origin/main`. **Spec author:** `FHE-TASK-DASHBOARDS-A`, 2026-09-03. Model/effort: MGMT decides (D45).

**MUST MERGE BEFORE:** B (E1, E2); B + D (E3 — the composed board needs the cycler). **E1 ∥ E2 are disjoint by DB object and by renderer file; they COLLIDE on `registry.ts` (both append to `DASHBOARDS`/`ELEMENTS`) and on `OwnerDashboard.tsx`'s renderer map** — each appends one clearly-fenced block (`/* ── E1 ── */`, `/* ── E2 ── */`); MGMT merges E1 first and E2 rebases (or serialises). **THE CONTRACT BINDS:** §2, §3, §4.3. **FIX6's BUILD ORDER IS NOT NEGOTIABLE:** *"ill need to see the sales and marketing dashboards first, so i know what to tell it to show on the ops dashboard."* — E1 ships → **STOP** → the owner reviews and supplies Claire's Ops list → E3.

⚠️ **Escalations 2 and 6 gate E1's CONTENT** (the metric list; whether Marketing exists before campaign data). MGMT appends the owner's answers under §1b before dispatch. **If he supplies no list, E1 builds the HONEST SET in §4b — every element either measurable today or labelled not-yet-measurable — and nothing invented.** 04-OPEN §3: *"Do not author the metric list yourself."*

**Zeroth act:** CLNR pass. **First act:** the ledger. **Second act:** re-run §2.

## 1. THE OWNER'S WORDS
- FIX6 §2b (2026-08-31): *"sales is a strict focus on the numbers that relate to revenue and could encompasse some of the conversion and traffic kpis, marketing would be the only unmentioned role scoped dashboard and the thing that would be nice for me is to be able to cycle through strict views of sales, marketing, ops, admin. this enables me to run campaigns and see a focused view of the results and things in the planning stages. Sales would be similar with focus on revenue, and ops would be similar with focus on overall company activities; horses, supplies, staff, things related to the ongoing daily tasks. Admin is higher level, things like ordering the company signage, updates to the app, investigating issues anyone is having with the app, monitoring website issues, etc..."* · *"the overlap is Ops for me and claire, that encompasses the things for trainer/instructor/care-taker/sales/marketing."* · *"i can list all of the things to include in the ops view."*
- 04-OPEN §2 (2026-08-26): *"the money is the only overlap we share … we might both want to see when a client claims an invite or when a contract is signed"* → **ONE zone rendered on BOTH boards.**
- 04-OPEN §3: *"things like conversion rates, number of form submissions, $ per website form submitted, $ per client, $ per conversion, are likely not ready to be calculated because the inputs are most likely not fully or properly implemented."* → a metric whose inputs are uncaptured says so.
- DAYSHEET §3 (2026-08-24): *"so shes not looking at a card that shows the day ahead and seeing things that already happened"* → a NEXT UP card, the forward list advances, done items below, each row clickable.
- DASHFEED §3: *"if the dashboard is going to do something like tell me i have a message, it better damn well show me the fucking message and i can click on it to respond without leaving the notification."*
### 1b. THE OWNER'S LIST (appended by MGMT after the summons — empty until then)
_(none yet)_

## 2. WHAT WAS MEASURED (2026-09-03; re-run)
| Fact | Where | Value |
|---|---|---|
| Money twice | `registry.ts` C3 `Money waiting` (`dash_money_waiting`: kinds `claim\|order`, carries **Confirm** via `confirmPaymentClaim`, `TrainerZones.tsx:144`) and B1 `Money that has not landed` (`dash_money_health`: kinds `declared\|unpaid_aging\|receipt_failed`, link-only rows, `BusinessZones.tsx:38`) | `claim` ≡ `declared` — the same purchases; B1's rows carry no act (half-measure) |
| TODAY does not advance | `dash_today_plan` → `lesson_plans_for_day(current_date)`, no time filter; `TrainerZones.tsx:46-77` renders all rows alike; `r.next_up` is the PLAN's next-up text, not a next-item card | the DAYSHEET residual is unbuilt |
| Revenue | `revenue_summary(p_from, p_to)` over `revenue_period_lines` (paid purchases at payment date; prior-window delta built in) | the Sales lens's core source; `windows.ts` supplies live windows, `period_bounds()` (B) the monthly ones |
| Attribution inputs | `lookup_options`: `client_origin` 6, `contact_channel` 5; `contacts.client_origin` / `contact_channel` exist (TASK-ORIGIN) — populated rows: `SELECT count(*) FILTER (WHERE client_origin IS NOT NULL), count(*) FROM contacts` → **re-measure; expected ~0 until the owner's backfill** | Marketing's only real input today |
| Traffic | no `page_events` table (`information_schema.tables` → 0); `@vercel/analytics` page-level only (RECONCILED 1.4) | **not measurable in-app**; B4 SITE / CR-106 |
| Campaigns | no table, no column (`grep -ri campaign supabase/migrations` → 0) | Marketing has no campaign data source — escalation 6 |
| App health inputs | `request_alert_sends`, `receipt_sends`, `signup_alert_sends`, `payment_request_sends` (send logs with `succeeded`/`error`), `support_requests`, `invitations.failure_reason`, `notifications.kind` last-seen per cron (`booking_reminder_1h` max `2026-09-02 12:03 -07`) | Admin desk's measurable set |
| Catalog hygiene | B8 `dash_catalog_hygiene` (service_types with 0 SKUs / no cover) | already an element; lists on `admin_desk` too |
| Existing keys | `ELEMENTS` after B: 16 | each new key is unique and never reused |

## 3. THE INCUMBENT, NAMED (D18) — CONVERGENCE
`dash_money_waiting` + `dash_money_health` → ONE new reader `dash_money()` and ONE element `MONEY` (the two old readers stay in the DB, D32; their registry rows retire — subtractive, sequenced in one commit with the test) · `revenue_summary` (Sales) · `dash_catalog_hygiene`, `dash_onboarding_pipeline` (Admin desk lists them; registered once) · `TodayZone` (advance) · the `Card`/`Cards`/`Tile` chrome · the chart renderers (new in E1, `src/components/app/dashboard/charts/`, the ONLY chart code — B5 uses them).

## 4. WHAT TO BUILD
### 4a. E1 — the shared fixes (first commit of E1; they are D18/half-measure debts the lenses inherit)
1. **`MONEY`** — migration: `dash_money() RETURNS jsonb {count, items}` = `dash_money_waiting`'s rows ∪ `dash_money_health`'s rows de-duplicated on `purchase_id` (a `claim` and a `declared` for the same purchase are ONE row of kind `claim`), kinds `claim | order | unpaid_aging | receipt_failed`, each row carrying what the two readers carry today (buyer, amount, code, method, reference, age, items, detail). Element `{ key:'MONEY', title:'Money', kind:'action', ask:'yours', domain:'money', dashboards:['trainer','business','sales'], order:30, source:{fn:'dash_money',shape:'zone'}, … to:'/app/ops/payments/review' }`. Renderer `MoneyZone` widened: `claim` rows keep **Confirm** (the act), `order`/`unpaid_aging`/`receipt_failed` rows link to payment review (their act lives there — say so in the hint, not silently). Retire C3 and B1 rows from `ELEMENTS` in the SAME commit as the test that the two boards show exactly one money element with the union's count. `REVOKE … FROM PUBLIC, anon` on `dash_money`; `proacl` pasted.
2. **Invite-claimed and contract-signed** (04-OPEN §2's other two overlaps): measure whether an element already carries each (B9 onboarding pipeline carries invitations; B3 carries documents). If yes, add `'trainer'` to their `dashboards` (registered once, listed twice); if a fact is carried by neither, register a `stat` element over `notifications.kind` counts for the last 7 days (`invite_claimed`, `party_signed` — measure the kinds that exist) — no new table.
3. **TODAY advances** (DAYSHEET residual): `TodayZone` splits `items` by `starts_at` vs `now()`: **NEXT UP** — the first future row as a prominent card (time, client, service, plan chip, "they left a note"); **Coming up** — the rest; **Done today** — past rows below, each still a link to its session (to add notes / act). Re-split on a 60s interval (the landing hook already polls at 60s; do not add a second timer — lift a shared `useMinuteTick`). The rule for "passed" = `starts_at < now()` (start time; DAYSHEET §3 Q1 asked start-or-end — start is the honest default because `ends_at` is null for most rows: measure `SELECT count(*) FILTER (WHERE ends_at IS NULL) FROM bookings WHERE starts_at::date = current_date`). "Does it show tomorrow?" (Q2): no — at day's end the zone is all Done and the footer says *nothing more today*; tomorrow is C2's first column. Both answers stated in the report as this task's decisions.
4. **Not-yet-measurable rendering**: the `stat` tile variant that prints *"Not yet measurable — <reason>"* in place of a number (`Tile` gets a `pending?: string` prop), and the report's figure line does the same. Every E1/E2 stat that depends on uncaptured inputs uses it.
### 4b. E1 — SALES and MARKETING (the honest set; the owner's §1b list overrides/extends)
- `DASHBOARDS.push({ key:'sales', label:'Sales', family:'lens', audience:'staff', owner:'B7', reportable:true, hint:'revenue, and the conversion and traffic numbers that feed it' }, { key:'marketing', label:'Marketing', family:'lens', … hint:'where clients come from, campaigns, what is still in planning' })`. Provision: tenant default rows `(NULL,'sales')`, `(NULL,'marketing')` are **NOT** seeded (a lens is opt-in — the owner holds it via the selector, D); seed `(admin@,'sales')` and `(admin@,'marketing')` only (FIX6: *"the thing that would be nice for me"*).
- **Sales elements** (all `cadence:'monthly'` unless said; sources are NEW `shape:'element'` RPCs, `(p_from, p_to, p_inputs)`, staff-gated, REVOKEd): `REV_MONTH` (stat, `raw_total | totals_over_months | line`, vsTotal, source `sales_revenue(p_from,p_to,p_inputs)` wrapping `revenue_summary` + `revenue_period_lines` grouped by month for the `months` input, default 6) · `REV_BY_OFFERING` (stat, `component_matrix | pie | stacked_bar`, by `purchase_items`' offering — measure the join `revenue_period_lines` exposes; if it does not expose the offering, say so and register `notYetMeasurable`) · `DECLARED_UNCONFIRMED` (stat, live, `raw_total`, from `dash_money`'s claim total) · `NEW_CLIENTS` (stat, `raw_total | totals_over_months`, contacts promoted to account per month — measure `contacts.created_at`/`accounts` promotion stamp; TASK-STABILIZE's activation) · `CONVERSION` (stat, **`notYetMeasurable`** unless `requests` → account promotion can be joined per month — measure; if the join exists, `raw_total` as a %, else the reason *"inquiries are captured; their promotion to an account is not stamped"*) · `TRAFFIC` (stat, **`notYetMeasurable` — "no page events reach the database (B4 SITE / CR-106)"**) · `MONEY` (listed, from 4a). Domain grouping: money first, then clients, then marketing.
- **Marketing elements**: `ORIGIN_MIX` (stat, `component_matrix | pie | stacked_bar`, source `marketing_origin_mix(p_from,p_to,p_inputs)` over `contacts.client_origin` × `contact_channel` for contacts created in the period; renders `notYetMeasurable` *"no client has an origin recorded yet — backfill pending"* while the populated count is 0 — the RPC returns it) · `CONVERSION` and `TRAFFIC` (listed again — registered once) · `CAMPAIGNS` (**escalation 6**: if the owner says Marketing exists before campaign data, register a `list` element with `notYetMeasurable` *"no campaign has been recorded — the campaigns record does not exist yet (proposal in the handoff)"*; if he says wait, do NOT register the `marketing` dashboard at all and say so).
- Each RPC's migration: REVOKE from `PUBLIC, anon`; each returns `{count, items, figures:[…], not_yet_measurable}`; **figures** register into `REPORT_FIGURES` for the Sales lens (`revenue_month`, `revenue_by_offering`, `new_clients_month`).
- Charts: `src/components/app/dashboard/charts/{Line,Bars,Pie,Matrix}.tsx` — SVG, no library (the app has none in `package.json`; adding one is a dependency decision — do not), the two data shapes of contract §9.2, theme from `index.css`'s `.dash-*` tokens, `prefers-reduced-motion` respected (as `CountUp` does). Variant switching reads `my_element_config` (B); the per-element variant CONTROL is chunk F — until F, the registry default renders.
### 4c. E2 — ADMIN DESK (independent of the STOP)
- `DASHBOARDS.push({ key:'admin_desk', label:'<escalation 6's name; default "Admin desk">', family:'lens', … hint:'the app, the website, the catalog, the company — the higher-level work' })`; seed `(admin@,'admin_desk')`.
- Elements (measurable today): `APP_SENDS_FAILED` (action, `yours`, `app`; source `admin_sends_failed()` — the four `*_sends` tables' failures in 30 days, each row → the record it was about) · `SUPPORT_OPEN` (action, `yours`, `app`; `support_requests` open — measure the table/status; the act = reply inline if a reply path exists, else link to `/app/ops/support` and say so) · `CRON_DEADMAN` (stat, `app`, live, `raw_total`; source `admin_cron_deadman()` — per scheduled endpoint the last observed EFFECT: `notifications.kind='booking_reminder_1h'` max, `document_deliveries` sweep max, `purchases`/`credits` mint stamp max — measure which stamps exist; renders age since last effect, amber over 2h for hourly jobs) · `B8` and `B9` listed on `admin_desk` too (registered once) · `INVITES_FAILED` if not already B9's (measure). **OUT (no CR):** the error-report inbox and the projects/tasks list (FIX6 §4–§5) — named in the handoff as proposals; do not build.
### 4d. E3 — OPS, composed (STUB; dispatched only with the owner's list in §1b)
- `DASHBOARDS.push({ key:'ops', label:'Ops', family:'lens', composed:true, … })`; seed `(NULL,'ops')` as a tenant default (FIX6: the landing board for both) **only when the owner's list has arrived**. `zonesFor('ops', held)` (B) yields the union; E3's work is the CLUSTERED rendering: numbers strip by domain, then YOURS · THEIRS · TODAY clusters with a heading each, items carrying their own subject (DASHFEED §5.2–5.3 — *"Kill the section titles. A cluster has a heading. An item carries its own subject."*). The role boards (Trainer beyond today's `trainer`, Instructor, Care-taker) also wait for this step (FIX6: no occupants today).

## 5. THE TRAPS
- **FIX6's STOP is real**: E1's report ends with *"STOPPED for the owner's Ops list"*; MGMT summons; E3 is never guessed.
- **Do not author the metric list** (04-OPEN §3) — the honest set is the FRAME the summons uses, not the answer.
- **A zero from an uncaptured input is worse than no tile** (04-OPEN §3, DASHFEED §4b) — `notYetMeasurable` everywhere it applies; the report lists which elements are pending and why.
- **Subtractive registry edits** (C3/B1 retire) land with their test in one commit (NOSTRIP).
- **Fresh functions inherit `anon`**; DROP+CREATE resets; `proacl` for every new RPC pasted.
- **Registered once, listed twice** — grep the registry for duplicate keys in the test.
- **CR-111**: no banned word in any label (the horse zone is titled *"The stable"* today — **do not rename it here**; it is CR-109/RANCHWORD's; note it).
- **Both E1 and E2 append to `registry.ts` and the renderer map** — fenced blocks; rebase, never re-order.
- **Charts**: SVG text must be readable on mobile (min 12px) and never the only carrier of a number — the matrix form is always one click away (A1 #11: *"on-hand detail lives one click away in a modal"* — the modal is the matrix).
- **Supabase errors / nested components / RLS** as every chunk.

## 6. OUT OF SCOPE
Claire's Ops list (his) · role boards Instructor/Care-taker · the error-report button and assigned-helper badge (FIX6 §5, §"helper" — proposals) · campaigns storage · `page_events` (B4) · renaming *"The stable"* (RANCHWORD) · element config UI (F) · B5's supplies elements.

## 7. WHAT YOU OWN / MUST NOT TOUCH
**Own (E1):** `registry.ts` E1 block · `TrainerZones.tsx` (`TodayZone`, `MoneyZone`) · `src/components/app/dashboard/{SalesZones,MarketingZones}.tsx`, `charts/*` (new) · `DashboardChrome.tsx` (`Tile.pending`) · `OwnerDashboard.tsx` renderer map E1 block · migrations for `dash_money`, `sales_*`, `marketing_*` · `test/db/dashboards_e1.test.ts`. **Own (E2):** `registry.ts` E2 block · `AdminDeskZones.tsx` (new) · migrations `admin_*` · `test/db/dashboards_e2.test.ts`. **Own (E3):** `OpsBoard.tsx` (new, the clustered composer) · registry E3 block. **DB:** the new RPCs only. **Not yours:** `dash_money_waiting`/`dash_money_health` bodies (left; D32) · `revenue_summary` · `AppLayout.tsx` · any supplies table · `contacts`.

## 8. THE REACH · THE TELL · THE SHAPE
- **REACH:** a lens appears in the cycler once held (D's selector; `admin@` is seeded for Sales/Marketing/Admin desk). Each element's `to` is a real route (`App.tsx`) — test greps them. The matrix modal opens from any chart tile.
- **TELL:** Sales shows revenue for last month with the prior-month delta and a six-month line; a pending tile reads *Not yet measurable — …* rather than 0; the two owner boards each show ONE money element; Today shows NEXT UP, then Coming up, then Done today.
- **THE SHAPE (owner's eyes — E1/E2 are new dashboards):** a lens page = the same header (cycler shows *Sales ▾*) · numbers strip grouped by domain (money tiles first: *Revenue · August — $X ▲ $Y vs July* · *Declared, unconfirmed — $Z* · *New clients — n*; then pending tiles greyed with their reason) · a chart element renders as a card with its title, the chart, a *Table* link opening the matrix modal · then action clusters (MONEY) · footer. Empty case: a lens with every element pending shows the strip of pending reasons and the footer *"nothing else to show yet"*. Error case per element (inline `ZoneError`).

## 9. THE TEST THIS MUST PASS
1. E1: `test/db/dashboards_e1.test.ts` — `dash_money` de-duplicates a purchase that is both a claim and declared; `sales_revenue` for a fixture month equals `revenue_summary` for the same window (D18); `marketing_origin_mix` returns `not_yet_measurable` when no contact has an origin and rows when one does; `proacl` of every new RPC has no `anon`; the registry has no duplicate element keys and no dangling `to` (a Vitest that imports `ELEMENTS` and `App.tsx`'s route table — the `pagevis_registry.test.ts` idiom).
2. Browser as `admin@`: hold Sales (D) → the strip shows revenue for last month equal to the Payment review page's month figure to the cent (same RPC); TRAFFIC/CONVERSION tiles read *Not yet measurable* with their reasons; switch to Claire's Dashboard → exactly one money element (`data-testid="zone-MONEY"`), C3/B1 absent; Today shows NEXT UP / Coming up / Done today at a time of day where all three exist (or say which were empty).
3. E1's report ends: **"STOPPED for the owner's Ops list (FIX6 build order)"**, and lists the pending elements as the FRAME for escalation 2.
4. E2: `admin_cron_deadman` shows an age ≤ 2h for the hourly job on production (the GitHub Actions job is running — `gh run list --workflow=scheduled-jobs.yml`); `admin_sends_failed` count matches `SELECT count(*) FROM (…the four tables' failures…)`; as a `USER`, all raise.
5. `typecheck` · `typecheck:api` · `lint` · `build`.

## 10. WHERE THE REPORT GOES
`docs/reports/TASK-DASHBOARDS-E1-REPORT.md` / `-E2-` / `-E3-`; two-line close to `FHE-MGMT-DASHBOARDS`. Read-only on production except migration applies; never `~/Desktop`; delete nothing; stage explicit paths; do not push; no subagents; TEARDOWN.
