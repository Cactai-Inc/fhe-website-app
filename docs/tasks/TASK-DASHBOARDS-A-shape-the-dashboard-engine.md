# TASK-DASHBOARDS-A — shape the dashboard engine, the many-dashboards door and the report machinery (DSNR profile: contract + specs + chunk declaration, no build)

**Thread:** `FHE-TASK-DASHBOARDS-A` · **Profile:** `DSNR` (`docs/method/DSNR-ROLE.md` binds you; `docs/method/TASK-ROLE.md` first) · **Model:** Fable 5.1 · effort HIGH · **Worktree:** `wt-8` · branch `task/dashboards-a` from `origin/main` · **Sender / hand back to:** `FHE-MGMT-DASHBOARDS` · **Bundle:** `docs/orch/BUNDLE-DASHBOARDS.md` (B7). Dispatched by MGMT 2026-09-03.

**Zeroth act:** the CLNR pass (`docs/method/CLNR-ROLE.md` §3) — one line if clean. **D36 guard, same turn as the claim:** `wt-8` must be detached with empty porcelain (MGMT saw it so at 2026-09-03: detached at `7fcf2188`, porcelain 0); then `git fetch origin && git checkout -b task/dashboards-a origin/main` and `git clean -xdf -e node_modules -e .env -e .env.db`. **Open `docs/reports/FHE-TASK-DASHBOARDS-A-LEDGER.md` as your first write and keep its RESUME block current.**

**READ-ONLY against production** (connection string: line 1 of `.env.db`). You measure; you never write a row. No code. No migration. No subagents.

---

## 0. ⚠️ THE ORDER OF YOUR OWN WORK — the contract goes UP before anything else
B5 SUPPLIES (`FHE-MGMT-SUPPLIES`, running now) specs its dashboard / projection / deviation / report tasks as **consumers** of the engine this bundle defines, and GATES their build on its merge. **So your first deliverable is `docs/design/DASHBOARD-ENGINE-CONTRACT.md`, committed on its own before any spec** — with a status line in its header: `STATUS: DRAFT` while you are still moving it, `STATUS: STABLE` the moment a consumer could build against it without you changing a name. **When you flip it to STABLE, say so in your ledger's RESUME block** (`CONTRACT: STABLE @ <commit>`). MGMT reads your branch, lifts that one file into `bundle/dashboards` and sends it UP to ORCH immediately — before your specs are finished. A contract amended after STABLE gets a `CHANGES` section at its foot naming what moved and why; consumers re-read it.

## 1. WHAT THIS TASK PRODUCES
1. **`docs/design/DASHBOARD-ENGINE-CONTRACT.md`** — the global, plug-and-play machinery of CR-112·A1 #11–#13, stated so that any dashboard (the owners' boards, the supplies dashboard, a client's board under HOMESHAPES) plugs in without a second mechanism (D18): **what a dashboard is · what an element is · an element's INPUTS and DISPLAY VARIANTS** (raw total · totals over N months · component matrix · pie · stacked bar · line, all vs total) · **element registration** (how a bundle like B5 declares an element and its data source) · **per-ACCOUNT provisioning from a tenant default** (changes saved to the account, never to the tenant) · **cadence** (monthly, not month-to-date; month-over-month, quarter-over-quarter, same-period-last-year, user-defined periods) · **the report generator** — a report is a SNAPSHOT of the user's dashboard as they see it + explicit figures of business importance regardless of dashboard + a per-entity statement (the per-horse statement is B5's instance) + a business monthly snapshot · **the generate modal shape** (parameters on the primary surface, inclusions from the element list in a large modal/page, me/both-accounts toggle, email + store, PDF/CSV/both, default = digital copy to documents) · **storage** — where a report lives and how it reaches the company "my documents" page · **naming and supersession** (#13: backdated data → old report renamed "outdated"; regeneration → old renamed "superseded" and archived-retrievable, the new one reuses the original name) · **idempotency** of generation · **what the engine does NOT decide** (content of any particular dashboard). Name the incumbents it converges (`src/lib/dashboard/registry.ts` ZONES, the `dash_*` reader RPCs, `profiles.dashboard_focus`, `files`/`file_links`).
2. **A spec SET** in `docs/tasks/TASK-DASHBOARDS-<LETTER>-<slug>.md` — letters from **B** onward (**A** is you; **V** and **W** reserved for the verifier and the walk). Anatomy: `DSNR-ROLE.md` §4, every section, **including THE SHAPE for the dashboards page, the selector and the report modal — these are standards every later dashboard inherits, and the owner must see them before build** (bundle §Gates to ORCH). Anything a client would see gets THE SHAPE too.
3. **The handoff** `docs/reports/FHE-DSNR-DASHBOARDS-HANDOFF.md` per `DSNR-ROLE.md` §5 — **plus** §6 below (the escalation evidence, one section per point) and §7 (the consumer interface B5 reads).
4. **The chunk declaration**: per chunk, the files, routes, DB objects (tables · functions · triggers · policies · storage buckets if any) it OWNS; what must merge before it; **which chunks are DISJOINT and may build in parallel**. Yours (architecture); MGMT only splits/merges for contention.
5. `docs/reports/TASK-DASHBOARDS-A-REPORT.md` (`TASK-ROLE.md` §6) and your ledger.

You do not build. You do not talk to the owner — questions go in the handoff as ASK-OWNER (most-blocking first) and MGMT summons him **once, batched**, on the six pre-registered points.

## 2. READ, IN THIS ORDER — what is RULED and what is still open
1. `docs/orch/BUNDLE-DASHBOARDS.md` — items, OWNERSHIP DECLARATION (§4 below), the six escalation points, gates, merge lane.
2. `docs/reference/CHANGE-ORDER-LEDGER.md` §CR-107 (many dashboards + an accessibility selector for the two owner accounts; the D13 note: a which-dashboards selector is ACCESS CONTROL, not arrangement) and §CR-112·A1 **#11, #12, #13** (verbatim — **these are the requirements for the engine**), then §CR-112·A1·THE PROPOSED LIST items 10–12 (ORCH's proposals the owner's answers refer to; #12's field-list shape was REJECTED — read A1 #12 for the shape that replaced it) and §CR-113 (the notification/digest scheduler the report reminder will lean on — B11's, not yours; name the dependency).
3. `docs/design/DASHBOARDS-GROUND-UP-PLAN.md` — the held plan. **Never approved as written.** Revisit against CR-107/CR-112; keep what survives, strike what does not, and say which in a `## REVISITED 2026-09-03` section of the plan (you own that file) or supersede it with the contract — your call, said aloud. Its §5 tasks/reminders substrate is **NOT in any CR — do not spec it**; list it as a proposal in the handoff.
4. `docs/tasks/TASK-DASHBOARDBUILD-phase1-two-real-dashboards-one-toggle.md` + `docs/reports/TASK-DASHBOARDBUILD-REPORT.md` — what IS built (two D26 boards, 16 zones, `dashboard_focus`, `set_dashboard_focus`, `revenue_summary`, landing rule). §9 lists what it flagged.
5. `docs/tasks/TASK-FIX6-ops-and-sales.md` — **absorbed, never ran, do not run as written.** Its §2b carries owner words that ARE requirements for this bundle: the held-views SET model ("a dashboard view is PROVISIONED to an account — any number — and the toggle appears only when an account holds more than one"; "this applies to me and to claire"), the seven views in two families (business lenses Sales · Marketing · Ops · Admin cycled by one person; job roles Trainer · Instructor · Care-taker), **Ops is COMPOSED never authored**, the **build order: Sales and Marketing first, then STOP for Claire's Ops list, Admin independent, Ops last**, and the "Admin" naming collision with the nav section (raise, do not pick). Its error-report button and assigned-helper badge are OUT of this bundle (no CR carries them) — list them as proposals. **Reconcile FIX6's held-views SET with CR-107's "selector": they are the same requirement said twice — one model, say which words survive.**
6. `docs/tasks/TASK-DASHFEED-cluster-by-what-it-asks-of-you.md` — the two-axis ruling (§4b: STATS group by DOMAIN, ACTIONS cluster by the ASK: YOURS · THEIRS · TODAY) and the half-measure rule (§3: an item that announces a thing carries the thing and the act). These shape what an ELEMENT is. Then `docs/method/04-OPEN-QUESTIONS.md` §1–§3 — the "three owner questions" DASHFEED was blocked on: **§1 messaging is ANSWERED** (one store, three views, sequenced after T3 — not this bundle's; the engine only needs an element that can render a thread inline), **§2 the two boards STAY** (two roles, not two views; shared zones — money, invite claimed, contract signed — render on BOTH), **§3 the metric list is still owed by the owner** (= escalation 2). Restate all three verbatim in §6.3 and say which are closed.
7. `docs/tasks/TASK-HOMESHAPES-four-account-types-one-composable-home.md` — design input only: a client's home is a dashboard under the same engine (one home, zones by what the account holds). Do not spec the member zones' content; make sure the engine can hold them.
8. `docs/tasks/TASK-DAYSHEET-claires-day-the-emails-around-it-and-the-calendar-that-shows-it.md` (1,327 lines) — **residual check only**: `api/calendar-reminders.ts` sends the 07:00 Pacific day sheet; dispositions completed/cancelled/expired/no_show are live; `TrainerZones.tsx` carries "next up". Decide whether anything of "Claire's static advancing day view" remains unbuilt; if yes, it is an element under the engine; if no, say so. Then **archive the file** (a docs move under `docs/archive/`, allowed — it is this bundle's file) with a one-line pointer where it was.
9. `CLAUDE.md` — D13 **and its recorded exception** (a self-arranging surface needs no ARRANGEMENT editor — dashboard_prefs is ruled out, not deferred; the exception is about zone arrangement only, not content or rules), D17, D18, D19, D20, D21, D25, D26, D32, D35, D36, D39, D41, D43 (+CR-111 banned words), D44.
10. `docs/reference/MODEL-CHOICE-NOTES-2026-09-01.md` §2026-09-03 — why this is a Fable task: **the question is the SHAPE**. Give build threads the outcome, the incumbent, the rulings and the traps.
11. The code and the record: `src/lib/dashboard/registry.ts` (ZONES, `DashboardView`, THE REACH helpers) · `src/components/app/dashboard/*` (`DashboardChrome`, `TrainerZones`, `BusinessZones`, `NotificationsZone`) · `src/pages/app/ops/OwnerDashboard.tsx` · `src/lib/pageRegistry.ts` (`mgmt.dashboard`) · `docs/reference/DB-MAP.md` rows for `_waiting_items`, `dash_waiting_on_you`, `dash_waiting_on_clients`, `dash_activity_readback`, every `dash_*` · `docs/reference/ACTIVITY-LOG-why-it-has-no-surface.md` · the `files`/`file_links` spine (`docs/tasks/TASK-UPLOADS-*`, memory 2026-08-11) · the My Documents page and `my_documents()` (the incumbent for "a documents page") · `docs/reference/FLOW-MAP.md` §1 (F15 document delivery, F18 account, F20 notifications).

## 3. THE OUTCOME, in one paragraph (quote the ledger in the specs; do not paraphrase it)
One engine, global and plug-and-play: dashboards are declared, elements are registered with their inputs and display variants, each ACCOUNT is provisioned its set of dashboards from a tenant default and its changes stay on the account, the selector on the dashboards page decides which dashboards each owner account can reach (access, not arrangement — allowed under D13's exception), the board self-arranges (no per-zone editor — ruled out), and a "monthly report" button generates an idempotent report — snapshot of that user's dashboard + explicit figures + per-entity statement + business snapshot — into BOTH owner accounts in their variants, stored where a company "my documents" page (shared by both owners, never co-mingled with client documents) lists it, emailed on request, as PDF/CSV/both, renamed "outdated" when backdated data arrives and "superseded"+archived on regeneration. B5's supplies dashboard, projections, deviations and report are the first consumers; a client's home (HOMESHAPES) is a later one. FIX6's held-views SET and view families are the provisioning model, with its build order (Sales/Marketing → STOP for Claire's list → Admin → Ops composed last) preserved in the chunk sequence.

## 4. THE OWNERSHIP BOUNDARY — copy it into every spec's "what you own / what you must not touch"
From `BUNDLE-DASHBOARDS.md` §Ownership: **DB** `dash_waiting_on_you` · `dash_waiting_on_clients` · `_waiting_items` · every NEW table/function for dashboard config, element config, per-account provisioning, report generation and report storage. **Reports are "stored in documents" (CR-112 #12) — declare the EXACT storage objects in the contract before any task applies them.** The `files`/`file_links` spine exists; ⚠️ **the contract-system `documents` table is under the signing freeze — no new columns on it without ORCH's word** (route that as an escalation to MGMT if the shape needs it). **Files** `src/lib/dashboard/**` · `src/components/app/dashboard/**` · `src/pages/app/ops/OwnerDashboard.tsx` · the dashboards page + selector (new) · the company "my documents" page (new) · `api/calendar-reminders.ts` ONLY if the DAYSHEET residual finds something · `docs/design/DASHBOARDS-GROUND-UP-PLAN.md` · `pageRegistry.ts` rows for the new pages.
⚠️ **NOT ours:** supplies data and the supplies dashboard's CONTENT/figures (B5 consumes the engine) · `AppLayout.tsx` nav (B10) · the requests inbox zone's CONTENT (B6 — we provide the slot and the contract) · `page_events`/`client_errors` beacons (B4 SITE / CR-106; a tile READS them later) · the tasks/reminders substrate (plan §5 — not in any CR; proposal only) · messaging convergence (04-OPEN §1 — after T3) · the notification/digest scheduler (B11 / CR-113). **A shape that needs a file or DB object outside this list is NOT specced around silently — name it in the handoff as an escalation for MGMT to take to ORCH, with what it needs and why.**

## 5. FACTS TO RE-MEASURE BEFORE YOU WRITE (D20 — every number carries the query you ran today)
- ⚠️ **How many owner accounts exist in production** (escalation 1): staff/admin profiles in FHE's org joined to `auth.users` emails; `profiles.dashboard_focus` per row. Known-inherited (verify): DASHBOARDBUILD seeded `hello@` → trainer and `admin@` → business (two rows); `TASK-AR1-REPORT.md:44` says Claire works inside `hello@` and CJ inside `admin@`; the memory-class "shared-login audit gap" says actor stamps under `hello@` cannot tell the two owners apart. **Which is it today — one login or two? Is `hello@` shared by both people?** The answer decides whether "per-account provisioning for Claire and myself" has a second account to provision.
- `profiles.dashboard_focus` and its CHECK (`'trainer'|'business'`); `set_dashboard_focus` call sites (FIX6 counted exactly one, `TeamPage.tsx`); the `sessionStorage['fhe.dashboard.view']` toggle. FIX6's recommendation — a `dashboard_views text[]` SET beside the retained `dashboard_focus` landing preference, values in `lookup_options` — is a recommendation, not a ruling; decide it in the contract and say why.
- `ZONES` in `registry.ts`: count per view today (DASHBOARDBUILD reported 16; DASHFEED counted 17; B6 was removed 2026-08-31). Which zones are shared facts written twice (C3/B1 money — 04-OPEN §2 says one zone on both boards).
- `_waiting_items`, `dash_waiting_on_you`, `dash_waiting_on_clients`: bodies from `pg_proc`, `proacl`, callers (DB-MAP: none). ⚠️ **No migration on `main` creates them** (grep 2026-09-03: only docs mention them) — they were applied from the deliberately-unmerged `b9bc9edc` WaitingZones branch. Decide reuse-or-retire INSIDE the contract (D32: retire = leave, never drop); if reused, the chunk that adopts them must land their definition in a migration on `main`.
- `dash_activity_readback` retained (registry comment); `ACTIVITY-LOG-why-it-has-no-surface.md` names the conditions for earning a surface back — an element under the engine may be that surface; say so or not.
- The report SINK: `files` / `file_links` columns, RLS and who reads them; the My Documents page's reader (`my_documents()`, party-read fixes DOCVIS/PARTYRLS); the `documents` table freeze. Declare where a generated report row lives and how "company documents do not co-mingle with client documents" is enforced (a scope/owner on the file link? a separate list? — measure the incumbent before proposing).
- PDF and CSV incumbents: the renderer the contract system already uses for documents (name the function/route) and any CSV export in the app — the report generator rides them (D18).
- Schedulers: `pg_cron` is ABSENT; the hourly GitHub Actions job (`.github/workflows/scheduled-jobs.yml`) is the only cron that has ever fired (AR1); Vercel crons never ran until CRON_SECRET was fixed (DEALAUTO follow-up) — measure which endpoints run today. The auto-generated report for dashboards without manual inputs, its day-before reminder and its one-minute-before deadline ride an existing scheduler or are specced as GATED on B11 — say which, measured.
- **No tenant timezone exists (X6)**; the day sheet is hardcoded 07:00 Pacific in `api/calendar-reminders.ts`. Monthly cadence and "defaults to last month" need a month boundary — name whose.
- `lookup_options`: adding a new vocabulary KEY is allowlisted in three places (repo memory 2026-08-27) — find them; any vocabulary the engine seeds (dashboard names, display variants, periods) names them in its chunk. D13: a migration-seeded vocabulary with no editor is the pattern the decision exists to stop.
- Landing: `useStaffLanding()` in `src/lib/dashboard/landing.ts` — the dashboards page as "the door" (CR-107) must not fight it.

## 6. THE ESCALATION EVIDENCE — one section each in the handoff, in this shape: *what exists today · the diff · recommendation*
The bundle pre-registered six points; MGMT summons the owner once with all six after your handoff. Make each cheap and final:
1. **Two owner accounts?** The accounts that exist (§5 first bullet, with the query), the delta between "per-account for Claire and myself" and reality, two options (a second owner account · a per-person profile under one login), one recommendation. If two real accounts already exist, say so plainly and mark the point CLOSED-BY-EVIDENCE.
2. **The metric list (M2).** He said he had one in a chat thread (04-OPEN §3) and it never arrived. Frame the single ask with **the engine's element list**: for each element you can already register from live data, what it needs as input; for each metric class he has named (revenue, conversion, traffic, $ per form, $ per client, run rate, burn rate, months cash on hand), whether its inputs are captured today (an uncaptured input renders as a zero indistinguishable from a real zero — say which cannot yet be computed). The summons asks for the list ONCE against that frame.
3. **DASHFEED's three owner questions** — restate verbatim from `04-OPEN-QUESTIONS.md` §1, §2, §3; mark §1 and §2 as ANSWERED in the file (quote the rulings) and §3 as = point 2. If nothing remains open, say the point collapses into 2.
4. **Element-config vs D13.** CR-112 #11's ELEMENT CONFIG (inputs + display variant per element) is a per-element editor. The D13 exception excluded ARRANGEMENT editors only. Put the boundary in a three-row table — selector = access (allowed) · element config = content/formula (D13 says owner-editable) · zone arrangement (excluded, ruled out) — with a recommendation and the one question "in force?".
5. **The company documents page.** A new owner-only surface (shared by both owners; reports identifiable by owner variant in the name; shared documents like a lease appear in both). Its NAME (D43/CR-111 banned words apply; "French Heritage Equestrian" in business-sense copy) and its PLACE (Account? the dashboards page? Admin?). Measure the incumbents (My Documents, Records → Documents, Admin section) and give ONE recommendation. Note the "Admin" collision FIX6 raised.
6. **Anything CR-107 / CR-112 #11–#13 cannot settle** — ASK-OWNER, batched, each with a recommendation. Include: the seven view names and the "Admin" collision (FIX6); whether Marketing exists before any campaign data does (empty is not a finding, but an empty lens is a door to nothing); whether a CLIENT dashboard/report ships in this bundle or only the engine's ability to hold one.

## 7. THE CONSUMER INTERFACE — what B5 (and any later bundle) reads from the contract
A section of the handoff AND of the contract, written for a thread that has never seen this bundle: how a bundle **registers a dashboard**, **registers an element** (key, inputs, data-source shape — an RPC signature or a view; the display variants it supports; its cadence), **declares per-account defaults**, **contributes explicit report figures and a per-entity statement**, and **what the report generator calls**. Name every function/table/file a consumer touches and every one it must not. B5 has already written its side ("the interface this bundle needs") in `docs/reports/FHE-DSNR-SUPPLIES-HANDOFF.md` §7 if it has landed on `origin/main` or `bundle/supplies` by the time you write — read it if present, reconcile, and say where you disagree; if absent, write the interface from CR-112·A1 #11–12 and mark it `AWAITING B5 RECONCILE`.

## 8. STANDARDS THIS BUNDLE SETS — say so in the spec that sets each, and produce THE SHAPE
- **The dashboards page** (the door) · **the selector** (which dashboards this account can reach) · **the report modal** (parameters on the primary surface, inclusions in a large modal/page, me/both toggle, email/store, PDF/CSV/both). States, each audience, the empty case, the error case. **Flag in the handoff: owner's eyes before build.**
- **The element contract** — an element that announces a thing carries the thing and the act (DASHFEED §3); stats group by domain, actions cluster by the ask (§4b); D25 naming on staff surfaces; a zone with no destination shows no link (registry `to?` comment).
- **D19 on report generation**: states itself before it writes, records why (who generated, period, variant), and is reversible (the superseded copy is archived-retrievable, never deleted — D32).
- **D13/D21**: dashboard names, element display variants, periods, report naming templates — owner-editable in-app or the spec names the follow-up that ships the editor.
- **D17/D39 THE REACH and THE TELL** for every surface and every captured value (a stored per-account config with no surface that shows it is unfinished).
- **CR-114** (buttons on desktop, dropdown on mobile for main→sub-page navigation) is being set by B5's first sub-page; if the dashboards page reaches sub-surfaces, adopt the same element and name it.

## 9. TRAPS, named (cite the source in the spec that inherits each)
- **A fresh function inherits `anon`/`authenticated` EXECUTE via Supabase default privileges; `REVOKE FROM PUBLIC` is not enough; DROP+CREATE resets the ACL** (CLAUDE.md, TASK-BOOKS1/ORIGIN). Every new RPC's spec carries the explicit REVOKE from `anon` by name and a `pg_proc.proacl` proof — the bundle's VRFY re-checks every one.
- **Per-account config lands on the ACCOUNT, not the tenant** (CR-112 #11) — a spec that writes to an org-level row is wrong by construction; VRFY checks it on production.
- **Report generation must be idempotent** — regenerate the same period twice → one current report, one superseded; the renaming (#13) must work on regeneration, not only on first generation.
- **`UPDATE OF <col>` fires on the columns the STATEMENT names** (ORCHESTRATOR.md §3c) — any "backdated data → rename outdated" trigger proves the firing statement.
- **`CREATE OR REPLACE` with a new defaulted parameter overloads** — drop the old signature.
- **`remerge_from_fields` destroys clause docs** (repo memory 2026-08-25) — irrelevant unless a report rides the contract-document path; if it does, name the safe functions.
- **The `documents` table is frozen** (71+ EXECUTED documents are evidence) — reports do not become `documents` rows without ORCH's word.
- **Supabase errors are not `Error` instances** (TASK-ERRSWEEP) — surfaces use `useAsync.ts`.
- **A component defined inside another component eats one keystroke per render** — hoist to module scope.
- **RLS was the real "can't reach" bug more than once** (TASK-AUTHORITY) — every new table's spec states its policies, the org boundary, and the test that a client cannot read an owner's report.
- **No cron but the hourly GitHub Actions job has ever fired; no tenant timezone** (AR1, X6) — anything scheduled says which scheduler, measured.
- **Reachability is part of done** (repo memory: 8× correct code nothing reached) — the dashboards page gets a nav row or a named door; `pageRegistry.ts` rows are in this bundle's ownership; `AppLayout.tsx` is NOT — if a nav row needs `AppLayout.tsx`, that is an escalation to MGMT for ORCH, not a quiet edit.
- **Subtractive changes are unsafe mid-walk; additive are safe** (TASK-NOSTRIP) — retiring `dashboard_focus`'s CHECK or renaming a view value touches two live rows; sequence it inside one chunk with the data migration.

## 10. CHUNKING GUIDANCE (yours to decide; MGMT's expectation, not a prescription)
A likely partition — challenge it: **(a) ENGINE** — the contract, config tables (dashboards · elements · per-account provisioning), registry changes, the held-views SET + migration of the two live rows, M5 reuse-or-retire — **merges FIRST as one unit** (bundle merge lane; B5 builds against a merged interface); **(b) DOOR** — the dashboards page + selector + landing reconciliation + `pageRegistry.ts` rows; **(c) REPORTS** — generator, storage objects, PDF/CSV ride-along, the company documents page, naming/supersession; **(d) BOARDS** — Sales and Marketing lenses in full, Admin independent, then STOP for Claire's Ops list (FIX6's order; Ops composed last — a later task after the owner's list arrives); **(e) RESIDUALS** — DAYSHEET check + archive, FIX6 one-line disposition (docs). Say per chunk why it is one and not two, what merges first, and which pairs are disjoint by DB object and by file. Keep (a) small enough to merge within days — B5 is waiting on it.

## 11. MODEL AND EFFORT PER CHUNK — recommend in the handoff (MGMT decides)
Default CODR: Opus · HIGH · thinking ON. Sonnet only for a mechanical docs move. VRFY: Opus · HIGH · ON with production queries (per-account config on the account; idempotent generation; supersession renaming on regeneration; `proacl` on every new function) — write THE TEST so a verifier can re-run every claim. WALKR at close: the dashboards page as each owner account · generate a monthly report · the supersede path; flows by FLOW-MAP id where they exist (F15 document delivery, F18 account); WALKTEST fixture, never a real client.

## 12. NON-NEGOTIABLES
Read-only against production · never `~/Desktop` · delete nothing · stage explicit paths · commit as you go on `task/dashboards-a` (the contract FIRST, on its own commit) · **do not push** · no subagents · TEARDOWN census at the end. Touch only `docs/design/DASHBOARD-ENGINE-CONTRACT.md`, `docs/design/DASHBOARDS-GROUND-UP-PLAN.md` (revisit section or supersession banner), `docs/tasks/TASK-DASHBOARDS-*.md`, `docs/reports/FHE-DSNR-DASHBOARDS-HANDOFF.md`, `docs/reports/FHE-TASK-DASHBOARDS-A-LEDGER.md`, `docs/reports/TASK-DASHBOARDS-A-REPORT.md`, and the DAYSHEET archive move (`git mv` into `docs/archive/`, pointer left behind).

## 13. CLOSE (TASK-ROLE.md §5b)
```
Done. Report at docs/reports/TASK-DASHBOARDS-A-REPORT.md
Hand this back to FHE-MGMT-DASHBOARDS
```
If you are STOPPING ON A QUESTION, say the question in one or two lines instead — MGMT routes it.
