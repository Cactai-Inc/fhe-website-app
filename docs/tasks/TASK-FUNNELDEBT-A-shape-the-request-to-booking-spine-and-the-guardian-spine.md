# TASK-FUNNELDEBT-A — shape the request→activation→booking spine and the minor/guardian spine (DSNR profile: specs + chunk declaration + escalation evidence, no build)

**Thread:** `FHE-TASK-FUNNELDEBT-A` · **Profile:** `DSNR` (`docs/method/DSNR-ROLE.md` binds you; `docs/method/TASK-ROLE.md` first) · **Model:** Fable 5.1 · effort HIGH · **Worktree:** `wt-10` · branch `task/funneldebt-a` from `origin/main` · **Sender / hand back to:** `FHE-MGMT-FUNNELDEBT` · **Bundle:** `docs/orch/BUNDLE-FUNNELDEBT.md` (B2, RECONCILED §8). Dispatched by MGMT 2026-09-03.

**Zeroth act:** the CLNR pass (`docs/method/CLNR-ROLE.md` §3) — one line if clean. **D36 guard, same turn as the claim:** `wt-10` must be detached with empty porcelain (MGMT saw it detached at `2779ca2c`, clean, `.env`/`.env.db`/`node_modules` present); then `git fetch origin && git checkout -b task/funneldebt-a origin/main` and `git clean -xdf -e node_modules -e .env -e .env.db`. **Open `docs/reports/FHE-TASK-FUNNELDEBT-A-LEDGER.md` as your first write and keep its RESUME block current.**

**READ-ONLY against production** (connection string: line 1 of `.env.db`). You measure; you never write a row, never apply a migration, never `BEGIN` anything that is not immediately `ROLLBACK`ed. No code. No subagents. **Charlotte Caddell's and the Caddell family's records: READ ONLY, and cite them only as the worked example the bundle asks for** — the owner is handling that family himself.

---

## 1. WHAT THIS TASK PRODUCES
1. **A spec SET** in `docs/tasks/TASK-FUNNELDEBT-<LETTER>-<slug>.md` — letters from **B** onward (**A** is you; **V** and **W** are reserved for the verifier and the walk). Anatomy: `DSNR-ROLE.md` §4, every section — owner's words · measured (with the query) · THE INCUMBENT · THE TRAPS · out of scope · THE REACH · THE TELL · THE TEST · where the report goes — plus THE SHAPE for anything a visitor, member or guardian will see (the confirmation page, any provisioning surface change, any account-page change).
2. **The handoff** `docs/reports/FHE-DSNR-FUNNELDEBT-HANDOFF.md` per `DSNR-ROLE.md` §5 — chunks in dependency order, contention, model/effort per chunk, ASK-OWNER most-blocking first, what you decided that the bundle did not, shapes that need his eyes.
3. 🔒 **The escalation evidence file** `docs/reports/FHE-TASK-FUNNELDEBT-A-ESCALATIONS.md` — **one section per pre-registered point (§6 below), in the fixed shape *what exists today · the options · the recommendation · what is blocked until ruled*.** MGMT puts this file in front of the owner ONCE, batched. It must be readable by him in five minutes: numbers, the two emails' text, the one-paragraph diffs. Not a survey — a recommendation each.
4. **The chunk declaration** (inside the handoff): per chunk, the files, routes, DB objects (tables · functions · triggers · constraints · policies) it OWNS; what must merge before it; **which chunks are DISJOINT and may build in parallel**, and which chunks are **GATED on a ruling** (they wait for the summons) versus **buildable now** (ruled by the bundle already).
5. `docs/reports/TASK-FUNNELDEBT-A-REPORT.md` (`TASK-ROLE.md` §6) and your ledger.

You do not build. You do not talk to the owner — every question goes in the ESCALATIONS file (if pre-registered) or the handoff's ASK-OWNER section (if not), and MGMT routes it.

## 2. READ, IN THIS ORDER
1. `docs/orch/BUNDLE-FUNNELDEBT.md` — the seven items, the OWNERSHIP DECLARATION (§4 below), the seven pre-registered escalation points, the gates, the merge lane.
2. `docs/orch/RECONCILED-2026-09-02.md` §2 (F1–F7, 3.2a) and §7 item 2 (F3's constraint). ⚠️ Its numbers are 2026-09-02; §5 below has MGMT's 2026-09-03 re-measure; you re-run again.
3. `docs/reports/HANDOFF-SIGNBOOK-THREAD-2026-09-01.md` §2 (the four flags, the owner's *"these are not small"*), §3.2 (the confirmation-copy requirement and his correction: **account first, then documents**), §3.4 · `docs/reports/SIGNBOOK-FINDING-the-door-does-not-know-who-is-knocking.md` §2 (the three-state door ruling — the activation email's own contract).
4. `docs/reports/FINDING-the-guardian-declared-at-the-door-is-lost-at-provisioning.md` (§3 the defect, §4 the four revisions — verbatim; §5 what was not done) · `docs/tasks/TASK-DEPENDENT-the-guardian-buys-the-child-rides.md` (the owner's 2026-08-16 words; the D1–D3 model; C10's guardian-addressed delivery as the precedent to extend) · `docs/tasks/TASK-C10-minor-rules.md`.
5. ⚠️ **CAREPATH §C6 lives in `docs/tasks/TASK-CAREPATH-horse-care-enquiry-to-active-client.md` §C6** (the bundle says the CR ledger; it is not there). Read it whole: *"the submitter should get an email with the information they sent us"* — that email is the one F1 would collapse. Then `docs/reference/CHANGE-ORDER-LEDGER.md` CR-98, CR-99, CR-101·A1, and the C6b confirmation-screen ruling it cites.
6. `CLAUDE.md` — D32 (nothing removed; a relabel is an UPDATE and still the owner's call), D18, D19, D20, D35, D36, D39, D41, D44. `docs/reference/MODEL-CHOICE-NOTES-2026-09-01.md` §2026-09-03 — why this is a Fable task: **the question is the SHAPE of two spines that three threads already patched at the surface**.
7. Repo memory-class traps: `docs/reports/TASK-BOOKS1-REPORT.md:80`, `TASK-BACKDATE-REPORT.md:290` (a fresh function is anon-executable by default privileges; `REVOKE FROM PUBLIC` is not enough; DROP+CREATE resets the ACL — **B1 GRANTS owns every ACL; your specs say `CREATE OR REPLACE`, never DROP, and carry a `proacl` before/after proof as THE TEST's evidence, not as a grant statement**) · `docs/reports/TASK-ERRSWEEP-REPORT.md` (Supabase errors are not `Error` instances) · `docs/method/ORCHESTRATOR.md` §3c (`UPDATE OF <col>` fires on the statement's target list).
8. The code, by path — `api/request-activation.ts` (header comment: the three states, NO ORDER NO EMAIL) · `api/inquiry-confirmation.ts` · `src/lib/api.ts:120-146` (the three `postSend`s on one spine; the comment says why the activation outcome is NOT folded into the confirmation screen's read-out) · `src/pages/Confirmation.tsx:150-200` · `src/pages/SignStart.tsx:160-176` (`SendStateScreen`'s spam / add-us-to-contacts / contact lines — **read, never edit; B3 owns the file**) · `api/delivery-sweep.ts` and `.github/workflows/scheduled-jobs.yml:47` (hourly) · `supabase/migrations/20260822T0730_dealauto_4_one_email_carries_the_contract_and_its_bundle.sql` (`flush_held_executed_document_emails`) · `supabase/migrations/20260901T1640_every_advance_is_one_button_pressed_by_one_side.sql:118,:179` (`request_open_time`, `book_open_slot`) · `20260823T1110_pamela_provision_can_save_without_sending.sql` (`provision_client_invitation`, latest) · `20260825T1605_redemption_carries_the_contract.sql` (`redeem_invitation`, latest) · `20260807140000_secfix_s3_ensure_client_account_execute.sql` (`_ensure_client_account`) · `src/pages/app/Onboarding.tsx` (the guardian toggle at `:157-169`, `:488`; the minor path) · `src/lib/ops/api-status.ts:27,40,51` (the ONLY app readers of `status_events.entity_type` MGMT's grep found — confirm and widen the grep to migrations: views, dossier functions, `_waiting_items`).

## 3. THE OUTCOME, in one paragraph
A website order submission produces ONE coherent set of messages the owner has ruled on (activation first, documents after activation, the confirmation page and the email saying the same thing); the booking timeline is filed under its own noun with no writer left that files a booking as an offering and no constraint that forbids the right value; a member who reaches the booking request either has a `clients` row or is told, in his words, why not; and a guardian who declares an under-18 rider at the public door is still the guardian at provisioning, at redemption, and on the purchase and the booking that follow — the same spine the wizard already gets right (`attach_minor_to_guardian`, `guardian_contact_id`, C10 delivery), extended to the lead→client door and to commerce, not rebuilt.

## 4. THE OWNERSHIP BOUNDARY — copy it into every spec's "what you own / what you must not touch"
From `BUNDLE-FUNNELDEBT.md` §Ownership. **DB (bodies, never ACLs):** the `status_events` CHECK constraint (and `status_events_vocab`'s twin — see §5) · every writer that files a booking event · `request_open_time` · `book_open_slot` · the other `'no member profile'` guard sites IF healed · `provision_client_invitation` · `redeem_invitation` (guardian read) · a NEW payer/guardian column on `purchases` and/or `bookings` — **the spec DECLARES the exact column name(s); B5 SUPPLIES holds a different new column on `purchases` (horse attribution); never share a migration file with B5.** **Files:** `api/request-activation.ts` · `api/inquiry-confirmation.ts` · `api/delivery-sweep.ts` (the hold parameter) · `src/lib/api.ts` — the ONE call at `:142`, nothing else · `src/pages/Confirmation.tsx` · `src/pages/app/Onboarding.tsx` for the guardian path — EXCEPT the stale-comment lines `:106-108` and `:621`, which B1 holds.
⚠️ **NOT ours:** `SignStart.tsx` (B3 — read only) · any `GRANT`/`REVOKE` (B1) · `AccountHub.tsx` (B5) · the six-state request machine and `request_purchase_payment` (B6) · `CLAUDE.md` and the CR ledger (ORCH). **A shape that needs a file or DB object outside this list is NOT specced around silently — name it in the handoff as an escalation for MGMT to take to ORCH, with what it needs and why.** If the guardian work needs a door change in `SignStart.tsx`, that is a FINDING routed up, not a spec line.

## 5. FACTS TO RE-MEASURE BEFORE YOU WRITE (D20 — every number in a spec carries the query you ran today)
MGMT measured once, read-only, 2026-09-03 07:21 PDT. **Your starting point, not your evidence.**
- **`status_events` by `entity_type`:** offering 829 · document 397 · account 144 · order 60 · fulfillment 30 · lesson_plan 1 · **booking 0**. Of the 829 `offering` rows: **781** have an `entity_id` that is a `bookings.id` (13 of those bookings are soft-deleted), **0** have an `entity_id` that is an `offerings.id`, **48 match neither** — classify those 48 (hard-deleted bookings? holds? another table?). Latest `offering` row: 2026-09-03 03:18 — still being written. `status` values on the 829: pending 653 · scheduled 164 · cancelled 11 · completed 1. Columns: `id, org_id, entity_type, entity_id, status, detail, actor_user_id, created_at`.
- ⚠️ **The CHECK in production is NOT the one the bundle cites.** `20260826T1000_payments_are_records_with_numbers.sql:109,115` re-created both constraints with `'payment'` added; production reads `entity_type = ANY (ARRAY['account','document','order','offering','fulfillment','lesson_plan','payment'])`. The spec changes the constraint as it IS, and **`status_events_vocab` carries the twin constraint** — widen both, and say whether `booking` needs vocab rows (`is_true_status`, terminal states — the six-state machine is B6's; you name the vocabulary, B6 is told).
- **The writer:** `20260901T1530_the_booking_has_six_states…sql:50` says *"`trg_status_bookings` writes that into `status_events`"* — find where that trigger and its function are created (MGMT's grep for `CREATE TRIGGER trg_status_bookings` found nothing; the name may be generated), what maps `bookings` → `'offering'`, and every OTHER writer that files a booking event (`log_status_event` call sites: 28 migration files; `20260817T1300_lessonrequest_l3…`, `20260812T1600_bookwrite…`, `20260901T1420_signbook…` are the booking-adjacent ones). **Prove there is no second writer by reading bodies, not names.**
- **Readers of `entity_type`:** `src/lib/ops/api-status.ts:27,40,51` in the app; in the DB, every function/view that filters `status_events` by `entity_type` (dossier timeline, `_waiting_items`, the booking queue). A relabel breaks any reader that hardcodes `'offering'` for bookings — list them; the compatibility-read option in escalation 3 depends on this list.
- **F4 — who has no `clients` row:** 13 member profiles (role `USER`, not admin); **3 lack a `clients` row, all three `zz-test-*@example.invalid` fixtures.** No live member is trapped today. The 15 `'no member profile'` guard sites: `20260803030000_publish_open_slots.sql:19` · `20260901T1700…:32,:66` · `20260804140000_lessee_lesson_booking.sql:45,:91` · `20260815T2300_reviewq_m2…:46,:144` · `20260816T2300_creditalign_m3…:104` · `20260812T1600_bookwrite…:618,:677` · `20260816T1500_onboard_m6…:162,:286,:332` · `20260901T1640…:118,:179`. Establish which are live (latest definition per function) versus superseded, and what `_ensure_client_account` does that a heal would reuse (D18 — one writer).
- **F1 — the two emails as SENT:** render both templates with a real recent order-bearing request (read-only; do not send). Paste both texts into the ESCALATIONS file. Note `request-activation`'s three states (new · activation pending · has account) — the collapse must preserve all three destinations.
- **F2 — the hold:** the function's actual semantics (what "held" means, what releases it, whether the hold clock starts at first signature or last), the sweep's hourly cadence (so the effective window is 30–90 minutes), and the two failure cases with times: (a) a slow signer whose documents email fires before the order/booking exist; (b) an abandoned run whose documents wait. Measure how many held rows exist now and their ages.
- **F6 / DEPENDENT — the worked example:** the two Charlotte records the FINDING names (`c44de468`, `b2d7d4ea`), `requests.details` for the declaring lead, `contacts.guardian_contact_id`, `purchases.buyer_contact_id`, `bookings.client_id`, `is_minor_contact`, and Gabriella Olenik's records from TASK-DEPENDENT. READ ONLY. The spec says what a payer/guardian column would hold for each, and what `provision_client_invitation` / `redeem_invitation` would have done under each of the four revisions.
- **3.2a — the confirmation copy:** what `Confirmation.tsx` says today (`:167-190`), what `SendStateScreen` says (`SignStart.tsx:160-176`), and the owner's framing (SIGNBOOK §3.2): account FIRST, then rules/policies/waiver. The spec carries the exact copy and THE SHAPE (order vs bare-enquiry cases; send-failed case) — **guest-facing: MGMT sends the render up before merge.**

## 6. THE ESCALATION EVIDENCE — `FHE-TASK-FUNNELDEBT-A-ESCALATIONS.md`, one section per point, verbatim headings from the bundle
1. **F1** — *collapse the two emails into one, or keep both per CAREPATH §C6?* Prepare: both emails' text as sent today, the one-email draft (it must carry what C6 promised — "the information they sent us, including step 2" — AND the activation link with the three-state destination), the recommendation. Say what is subtractive (NOSTRIP) and what is not.
2. **F2** — *shorten/lengthen the 30 minutes, or make the sweep skip documents whose executed email is still being composed?* Prepare the two failure cases with times, and what "still being composed" would key on in the data that exists.
3. **F3** — *relabel the 781 live rows to `booking`, or leave them and fix only the writer + constraint + a compatibility read?* Prepare: counts re-measured (with the 48 and the 13 classified), the reader list, the rehearsal shape (`BEGIN … UPDATE … counts … ROLLBACK`), the recommendation. **The relabel is an UPDATE, not a removal — D32 permits it; it is still his call, and it merges only after his words are in the CR ledger.** Writer + constraint chunk must be buildable before the ruling.
4. **F4** — *heal (create the `clients` row at request time from the member's contact) or guard with a message?* Prepare: the count (3, all fixtures) and WHY those three lack a row (which door made them), the heal's incumbent (`_ensure_client_account`), and what the message would say in his voice if guard is chosen.
5. **F6** — *which of the FINDING's four revisions?* One-paragraph diff of each: what changes, in which function/surface, what it refuses, what it shows. Recommend a set (they are not mutually exclusive; say which combine).
6. **DEPENDENT** — *payer on the purchase, or guardian on the booking, or both?* Worked example: the first real minor's records (Charlotte — READ ONLY) and Gabriella's. Declare the exact column name(s) you would add and why (`purchases.payer_contact_id`? `bookings.participant_contact_id`? — the spec decides the vocabulary; B5's column on `purchases` is horse attribution, never the same name). TASK-DEPENDENT's open question rides here: is a guardian's credit ALSO bookable for the guardian, or only the dependent?
7. **Anything else the spec cannot resolve** — same file, same shape.

## 7. STANDARDS AND RULINGS THAT BIND EVERY SPEC
- **Owner framing (SIGNBOOK §3.2): account FIRST, then documents.** No copy, email or screen may imply signing before activation.
- **CAREPATH §C6 stands until he changes it** — the buyer confirmation email is his ruling; F1's collapse is his call, not the spec's.
- **D32:** nothing removed. Relabel = UPDATE with a rehearsal and counts before/after; hidden, never deleted.
- **D19 on the relabel and on any guardian routing:** states itself, records why, reversible (the old `entity_type` value survives in `detail` or a sibling column — the spec says where).
- **D18 THE INCUMBENT:** `attach_minor_to_guardian` · `guardian_contact_id` · `is_minor_contact` · C10 delivery · `_ensure_client_account` · `accountDoor.ts`'s three states. Converge; say so.
- **D39 THE REACH / THE TELL:** for every captured value (payer, guardian on a booking, a healed client row) — where it is SEEN and where it is ACTED ON. A column nobody reads is unfinished.
- **ACLs are B1's.** Your migrations `CREATE OR REPLACE` existing bodies (keeps the ACL); a genuinely NEW function's spec carries an explicit `REVOKE … FROM PUBLIC, anon` line **flagged for MGMT to clear with ORCH/B1 before apply** — say so rather than granting silently.
- **VRFY will re-run:** `status_events` counts by `entity_type` before/after; a guardian surviving provisioning on a WALKTEST fixture; `proacl` on every touched function. Write THE TEST so that is mechanical.

## 8. CHUNKING GUIDANCE (yours to decide; MGMT's expectation, not a prescription)
A likely partition — challenge it: **(B)** F3 writer + both constraints + compatibility read, buildable NOW, disjoint from everything; **(C)** F3 relabel migration, GATED on ruling 3, runs after B; **(D)** F1 + 3.2a — the one-message set (endpoints, the `api.ts:142` call, `Confirmation.tsx` copy), GATED on ruling 1, guest-facing render up; **(E)** F2 hold tuning, GATED on ruling 2, one file + one parameter; **(F)** F4 heal-or-guard, GATED on ruling 4; **(G)** F6 guardian at the door (`provision_client_invitation`, `redeem_invitation`, `Onboarding.tsx` guardian path), GATED on ruling 5; **(H)** DEPENDENT payer/guardian columns + writers + readers, GATED on ruling 6, after G. Say per chunk why it is one and not two, what merges first, which pairs are disjoint by DB object and by file, and which can be dispatched the day the owner rules.

## 9. MODEL AND EFFORT PER CHUNK — recommend in the handoff (MGMT decides)
Default CODR: Opus · HIGH · thinking ON. Sonnet only for a pure parameter/copy change with no judgment (E, possibly D's copy half). VRFY: Opus · HIGH · ON with production queries.

## 10. NON-NEGOTIABLES
Read-only against production · never `~/Desktop` · delete nothing · stage explicit paths · commit as you go on `task/funneldebt-a` · **do not push** · no subagents · TEARDOWN census at the end · tree left on its branch, clean (MGMT retires it). Touch only `docs/tasks/TASK-FUNNELDEBT-*.md`, `docs/reports/FHE-DSNR-FUNNELDEBT-HANDOFF.md`, `docs/reports/FHE-TASK-FUNNELDEBT-A-ESCALATIONS.md`, `docs/reports/FHE-TASK-FUNNELDEBT-A-LEDGER.md`, `docs/reports/TASK-FUNNELDEBT-A-REPORT.md`.

## 11. THE TEST THIS TASK MUST PASS
1. The spec set, the handoff and the ESCALATIONS file exist; each spec is self-sufficient (`DSNR-ROLE.md` §"YOUR SPEC IS THE ONLY THING").
2. Every number in §5 re-measured with the query pasted in your ledger; the 48 and the 13 classified; the booking-event writer(s) named from bodies.
3. The ESCALATIONS file has seven sections, each with a recommendation, readable in five minutes.
4. The handoff declares chunks, disjointness, what is gated on which ruling, model/effort per chunk, and every out-of-declaration need as a routed escalation.
5. `git diff --stat` on `task/funneldebt-a` touches only `docs/`.
6. TEARDOWN census in the report.

## 12. CLOSE (`TASK-ROLE.md` §5b)
```
Done. Report at docs/reports/TASK-FUNNELDEBT-A-REPORT.md
Hand this back to FHE-MGMT-FUNNELDEBT
```
If you are STOPPING ON A QUESTION, say the question in one or two lines instead — MGMT routes it.
