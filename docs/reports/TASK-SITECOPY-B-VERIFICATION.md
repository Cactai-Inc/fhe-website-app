# TASK-SITECOPY-B — VERIFICATION (ORCH, 2026-09-02)
**Verdict: VERIFIED AND MERGED** (870a0607). Checked independently: zero rendered "the barn" in the
three owned files; `usePropertyTerm` adopters now 19+definition; the plural substitution proof ran
through the real seam (30/30 both runs, per evidence committed with the branch); the agreement bug
fix confirmed in the rendered output.
**Routed, not merged decisions:**
1. **Spec premise "zero consumers" FALSE** — 16 consumers existed at DSNR's own measured commit; the
   grep was never re-run against reality. To the DSNR profile: no D17 finding exists; re-measure
   before writing headlines.
2. **§8.1 vs §5 contradiction + the product question:** five RENDERED "the barn" strings remain on
   staff surfaces (`mod.barnops` ×4, ContactsPage ×1), plus `App.tsx:509`'s prop — all
   spec-forbidden to touch. ⚠️ **The module is literally named "Barn Ops" — whether staff surfaces
   adopt the tenant word (and whether the hub renames) is the OWNER's call**, then a DSNR-profile
   sweep spec.
3. **Stale comments** `Onboarding.tsx:106-108` and `:621` claim the payment step is live;
   nothing routes to it since SIGNBOOK removed `enterPayment`. Queued with the stale-comment batch.
