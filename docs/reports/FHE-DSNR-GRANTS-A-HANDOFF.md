# FHE-DSNR-GRANTS-A — HANDOFF to `FHE-MGMT-GRANTS`

**From `FHE-TASK-GRANTS-A` (TASK thread wearing the `DSNR` profile), 2026-09-03, tree `wt-2`, branch
`task/grants-a-spec` from `origin/main` @ `2779ca2c`.**
**Bundle: `docs/orch/BUNDLE-GRANTS.md`. Ledger: `docs/reports/FHE-TASK-GRANTS-A-LEDGER.md`.**

## THE THREE FILES

| file | what it is | who reads it |
|---|---|---|
| `docs/tasks/TASK-GRANTS-B-close-the-anon-door-on-every-writer-nothing-anonymous-calls.md` | the build spec | `FHE-TASK-GRANTS-B` (CODR) — its only input |
| `docs/reports/FHE-TASK-GRANTS-A-ANON-WRITERS.md` | 🔒 **the escalation list** | **the owner**, via you |
| this file | the chunks, the decisions, the corrections | you |

---

## 1. 🔒 WHAT THE OWNER IS BEING ASKED — one block and two confirmations, not 145 questions

**The escalation list opens with the question and nothing else:**
- **Block A — 140 functions. REVOKE as one block?** No anonymous caller exists for any of them.
- **Block B — 2 KEEPs to confirm** (`submit_public_request`, `open_gift`) **and 1 recommended REVOKE**
  (`redeem_gift`, with the evidence set out so "no" costs him one word).

🔒 **`open_gift` is the finding the sweep existed to produce.** It writes `gifts` with **no in-body
guard**, it is called from `/redeem` **on mount, before the page reads `user`**, and it is **not** on the
bundle's known-public list. Had the sweep revoked by pattern, every gift recipient without an account
would have hit a dead reveal page.

**Put the file in front of him as it is.** It answers "what breaks if we revoke this" per row, and the
per-row evidence is a `file:line` he can have checked rather than a claim.

---

## 2. THE CHUNKS — two, and they are disjoint by construction

| # | chunk | owns | must merge before it | why it is one chunk and not two |
|---|---|---|---|---|
| **1** | `FHE-TASK-GRANTS-B` — the ACL migration **and** the four comment edits | one new file in `supabase/migrations/`; the named lines in `src/lib/contact.ts`, `api/deliver-document.ts`, `src/pages/app/Onboarding.tsx` | nothing | **Same seam: both are "the repo says something the database does not."** The comments are prose in three files nothing else in this bundle touches, they add no risk to a migration that is already going through the rehearsal discipline, and splitting them would cost a second thread, a second branch and a second merge for a 12-line diff |
| **2** | `FHE-TASK-GRANTS-V` (VRFY) then `FHE-TASK-GRANTS-W` (WALKR) | nothing — they read | chunk 1 merged and deployed | VRFY re-runs the `proacl` proof against production; WALKR walks the two anonymous surfaces. **WALKR needs the deploy, VRFY does not** |

**Contention:** none I can see. **No other bundle owns a function ACL** (B2 FUNNELDEBT owns function
BODIES, which this migration never touches — criterion 7 of THE TEST proves that with an `md5` of
`pg_get_functiondef` before and after). ⚠️ **The one file two bundles could collide on is
`src/pages/app/Onboarding.tsx`** — it is a large, much-edited file and this task owns **two comment
blocks** in it. If anything else is live in that file, take the two Onboarding edits out of chunk 1 and
run them last; the migration does not depend on them.

### Model and effort

| chunk | model | effort | thinking | why |
|---|---|---|---|---|
| `-B` CODR | **Sonnet** | **MEDIUM** | ON | The judgement is spent. The statements are GENERATED by a pinned query, the four comment texts are verbatim in the spec, and THE TEST names every query. This is execution and pasted proof |
| `-V` VRFY | **Opus** | **HIGH** | ON | It must re-derive the `proacl` table itself and catch a revoke that caught a KEEP — that is the failure mode, and it is invisible unless someone looks for it. The bundle's own suggestion, unchanged |
| `-W` WALKR | **Opus** | **HIGH** | ON | An anonymous browser walk of two funnels; the bundle's suggestion, unchanged |

---

## 3. 🔒 WHAT I DECIDED THAT THE BUNDLE DID NOT

1. **ONE migration file, not one-with-a-commented-Section-2.** Your constraint was that `-B` be
   dispatchable the day the owner rules on Block A. It is: **Block B produces at most ONE statement**
   (`redeem_gift`; the two KEEPs produce none), so there is no body of work waiting on the ruling.
   ⚠️ **And a commented Section 2 inside an applied migration can never be filled in — the file is
   immutable once applied.** If the `redeem_gift` ruling lands after `-B`, it is a two-line migration of
   its own. The spec says this explicitly so the build cannot improvise.
2. **`redeem_gift` moved from ASK to REVOKE.** Its first line is
   `IF auth.uid() IS NULL THEN RETURN 'not_authenticated'` — an anon call today writes nothing — and
   `registerForGift` (`src/lib/gifts.ts:54-66`) `await`s `signInWithPassword` before `Redeem.tsx:76`
   calls it. **Nothing that works today stops working.** It stays a Block B row so he can overrule.
3. **The trigger functions are revoked, all 46 of them, including the `event_trigger`.** The bundle
   said item 3 goes in "under their own heading"; I extended that to the whole inert class because the
   thing being fixed is a **false claim** (`TASK-SIGNBOOK-VERIFICATION.md`: *"anon confirmed absent on
   every migration"*), and a claim is only true when it is true of all of them.
4. **The 15 other `SECURITY INVOKER` trigger functions that carry `anon` are NOT in the migration.**
   They are outside the bundle's ownership declaration ("every `SECURITY DEFINER` function in `public`").
   `trg_seed_display_name` is in **because the bundle names it**, not because it is a definer — it is not.
   **This is deliberately a line, not a sweep: see §5 for the follow-up it leaves.**
5. **The statements are generated from a pinned NAME LIST, not from a predicate.** A predicate would
   pick up any function created between spec and build. The 142 names are in the spec; if the build's
   own sweep disagrees, that is a finding it reports before building.
6. **The two Onboarding replacement texts** (nobody supplied them) are written from the code, and each
   cites the in-file comment at `:335-340` that already says the correct thing — so the fix is the file
   agreeing with itself, not my opinion of what the flow does.

---

## 4. ⚠️ WHERE THE BUNDLE AND ITS LEDGER WERE WRONG — four, all measured

1. 🔒 **`trg_seed_display_name` is `SECURITY INVOKER` (`prosecdef = f`).** The MGMT ledger's MEASUREMENT
   section says *"(`trg_seed_display_name` row: see the -A task's own re-run — it is in the 45.)"*
   **It is not.** The 45 are all `prosecdef = t`; this one is not in the 326 population at all. It is in
   the migration by name, and the spec flags this so the build does not go looking for it in the predicate.
2. 🔒 **`request_category_label` is not a writer, not a definer, and has no call site.** The bundle names
   it beside `submit_public_request` as *"known-public by design … (the contact form)"*. Measured: it is
   `SECURITY INVOKER`, `IMMUTABLE`, its whole body is a `CASE` mapping `'visit'` → `'Visit the ranch'`,
   and `grep -rn "request_category_label" src api` is **empty** — it is called from inside another SQL
   function. **It is not on the escalation list and nothing in this bundle touches it.** (Your ledger's
   dispatch note had already found half of this; the other half is that it writes nothing.)
3. 🔒 **The sign-start writers do NOT land in Block B.** The bundle predicted *"the sign-start writers
   (`sign_start_register_attempt` and whatever else `/sign/*` calls logged out) and the gift/redeem doors
   will land here."* `src/pages/SignStart.tsx:449` calls **`/api/sign-start`**, and
   `api/sign-start.ts:198` uses `getSupabaseAdmin()` — **`service_role`**. `sign_start_register_attempt`,
   `apply_sign_path_documents` and `provision_client_invitation` have no anonymous caller and are plain
   Block A. **The gift door half of the prediction was right, and it was right about the wrong function:
   `open_gift`, not `redeem_gift`.**
4. **Item 6 is FOUR comment edits, not five.** `MergedBodyView.tsx`'s stale `Release.tsx` comment was
   already fixed by `d78d3b3c` (verified with `git log -L35,36`). The spec tells the build not to touch
   that file and criterion 10 of THE TEST proves it did not.

**Not wrong, but worth recording:** the bundle's candidate set — the 151 anon-executable volatile
non-trigger functions — is **not** the writer set. It contains readers (`claim_request_alert_send`,
`require_module`, `dm_list_conversations`, `dm_unread_total`, `feed_get`) and the real writer count is
**145**, found by reading all 326 bodies. Your ledger's `DO NOT` line already said not to treat
`provolatile='v'` as "writes"; this is the measurement behind it.

---

## 5. 🔒 WHAT THIS BUNDLE LEAVES BEHIND — for ORCH to bundle, NOT for `-B`

Each is one line, per CR-94. **None of these blocks anything.**

- **135 anon-executable `SECURITY DEFINER` READERS remain** (plus **60** anon-executable
  `SECURITY INVOKER` non-trigger functions and **15** invoker trigger functions). **A read-ACL sweep is
  a separate bundle**; this one deliberately does not widen to it. The classification that produced the
  135 is in `docs/reports/FHE-TASK-GRANTS-A-ANON-WRITERS.md` and does not need re-deriving.
- ⚠️ **14 of the 145 writers have NO in-body guard at all** — no `has_staff_access()`, no `auth.uid()`,
  no org fence: `_restore_contract_template_composition` · `apply_offering_documents` ·
  `apply_sign_path_documents` · `complete_deal` · `log_mirror_delivery` · `log_request_alert_send` ·
  `open_gift` · `reap_expired_holds` · `reopen_change_request` · `resolve_change_request_thread` ·
  `rls_auto_enable` · `set_lesson_progress_note` · `supersede_invitations` · `upsert_content_block`.
  **Revoking `anon` closes the anonymous door; it does not add a guard, and any `authenticated` caller
  still reaches them unguarded.** **That is a BODY question and belongs to B2 FUNNELDEBT** (bundle:
  *"if the sweep finds a body needs a guard, that is a FINDING routed up, not a fix"*). This is that
  finding.
- **`src/pages/app/Onboarding.tsx:632`** — *"Step 3 — payment (sign-before-pay)"* is the same stale
  claim as the two comments this bundle fixes, on a line the bundle does not name. **Left alone** (the
  ownership is "the five comment lines … nothing else in those files"). One line for the next stale-comment pass.
- **RECONCILED §8 row B1 also lists 1.15, 1.19 and §7.6**, which `BUNDLE-GRANTS.md` does not carry.
  Your ledger already routed this to ORCH; nothing changed.

---

## 6. ANYTHING THAT STILL NEEDS THE OWNER — ASK-OWNER, most blocking first

1. 🔒 **Block A: revoke all 140 as one block? (blocks `-B` entirely.)** The file is written to be ruled
   in one pass.
2. **Confirm the two KEEPs** — `submit_public_request` and `open_gift`. A "no" on `open_gift` breaks
   `/redeem` for anyone without an account, which is why it is a confirmation and not an assumption.
3. **`redeem_gift`: REVOKE, recommended with evidence.** Not blocking — if he does not rule, `-B` ships
   without it and it becomes a two-line migration later.

**Nothing here needs his EYES on a shape.** Nothing renders; there is no new page, no new state a user
sees, no layout. `DSNR-ROLE.md` §4's "when the work changes a surface's shape" does not apply.

---

## 7. THE ZEROTH ACT, AND THE TREE

**CLNR pass (`CLNR-ROLE.md` §3): clean.** `docs/` obeys §2a — no loose files at the root, one folder per
type, 286 reports, the four role files plus `MGMT`/`VRFY`/`WALKR`/`CODR` all present in `docs/method/`,
the §2b resumability test PASSES for every role. Nine worktrees, all claimed or detached-and-clean.
**Nothing moved — three bundles are live and CLNR never moves a file under a running thread.**

**`git diff --stat origin/main` on `task/grants-a-spec` touches `docs/` only.** Four commits, each
standing on its own. **Not pushed** — merging is yours (`MGMT-ROLE.md` §3).
