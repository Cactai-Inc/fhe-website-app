# Orchestrator handoff — MOVED

**This file is no longer maintained. The canonical handoff is
[`SESSION_HANDOFF_2026-08-07.md`](SESSION_HANDOFF_2026-08-07.md).**

Two handoff documents existed briefly on 2026-08-07, written independently within half an
hour of each other. Two sources of truth drift — which is the same failure that put a stale
"the no-guard family is not yet tasked" instruction in front of an incoming orchestrator
while that thread was already running.

Everything unique to this file has been folded into the canonical one. **Do not create a
third handoff doc.** Update the canonical file in place, and put a dated CORRECTIONS block
at the top when it goes stale rather than starting over.

See also `docs/THREAD_REGISTRY.md` for thread IDs, status, and the confirmed prompt shape.
